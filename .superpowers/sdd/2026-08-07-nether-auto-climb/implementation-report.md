# Nether F12 auto-climb — implementation report

## Preflight

- Authorized base: `bc84a2f88fdb61c712aa20444dc2601c38cc447b` on the existing authorized `master` checkout. The checkout is not a linked worktree; the plan ledger explicitly authorizes this checkout.
- Preserved user state before implementation: modified `README.md`; untracked `build/`.
- Docker baseline: `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --configuration Release --nologo` passed `199/199`, `0` failed/skipped. The existing nullable warnings are in `TranslationPaths.cs`, `Manifest.cs`, and `StaticBundleProtocol.cs` (18 locations); no feature warning was observed.
- RO native preflight evidence: `Project.Nether.FloorSelection.SubViewController.OnFloorClickedEventAsync(int floorLevel, int floorIndex)`, `HandleStartEventByStatusAsync(bool)`, `ExecuteNextFloorMovementSequenceAsync(NetherFloorModel)`, `Project.Ingame.Exploration.NetherAPIService.StartQuestAsync/ClearQuestAsync/CloseQuestAsync`, and `Project.NetherTop.Result.SubViewController.CreateNetherResultModelAsync(bool, Il2CppReferenceArray<NetherResultPartyCharacterModel>)` are present in current `/game/BepInEx/interop/Project.dll`. Detailed Task 6 binding audit remains required before runtime mutation code. No raw API action will be substituted for unavailable native UI flows.

## Task 1 — pure snapshots and single-flight reconciliation state machine

- Base/head: `bc84a2f` → `5e7ebe056ea2d3c734ab3f3b89aba2cc20a8484c` (`feat: add nether auto-climb state model`).
- Precisely staged paths: `AbyssMod/Services/NetherAutoClimbModels.cs`; `AbyssMod/Services/NetherAutoClimbStateMachine.cs`; `AbyssMod.Tests/NetherAutoClimbStateMachineTests.cs`; `AbyssMod.Tests/AbyssMod.Tests.csproj`.
- RED: Docker `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --configuration Release --filter FullyQualifiedName~NetherAutoClimbStateMachineTests --nologo` exited `1` with `CS0246` for absent `NetherSessionStatus`, `NetherAutoClimbStateMachine`, and `NetherSnapshotFingerprint`. This proves the tested state-machine contract did not yet exist.
- GREEN focused: same Docker command passed `9/9`, `0` failed/skipped. It covers outside-Nether disable, one in-flight action, unknown-outcome reconciliation, applied/not-applied/ambiguous fingerprints, Clear/Lose awaiting Result, and F11 wait transitions.
- Full suite: Docker `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --configuration Release --nologo` passed `208/208`, `0` failed/skipped. Warning delta: none; only the pre-existing nullable warning locations appeared on the non-incremental focused build.
- Design notes: `TryBegin` stores the pre-action fingerprint and rejects a duplicate known-not-applied action at the same snapshot. Unknown outcomes retain the pending action and force `Reconciling`; a classifier must explicitly supply `Applied`, `NotApplied`, or `Ambiguous`. Clear/Lose move only to `AwaitingSceneChange`; `Complete()` is deliberately restricted to a subsequent Result observation.
- Concerns / runtime boundary: pure state tests cannot prove a live Unity controller callback; Task 6 must bind exactly one current-version native method and otherwise return `BindingUnavailable` / pause.

## Task 2 — server graph validation and safe deterministic route planning

- Base/head: `5e7ebe0` → `bc53c9e154a814a419e46abae8c718e1f4b9b3ab` (`feat: add safe nether route planning`).
- Precisely staged paths: `AbyssMod/Services/NetherRoutePlanner.cs`; `AbyssMod.Tests/NetherRoutePlannerTests.cs`; `AbyssMod.Tests/AbyssMod.Tests.csproj`.
- RED: Docker `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --configuration Release --filter FullyQualifiedName~NetherRoutePlannerTests --nologo` exited `1` with `CS0246` for absent `NetherRoutePlan` and `NetherRouteSafetyContext`. The literal graph tests therefore exercised a missing planner contract rather than pre-existing source text.
- GREEN focused: same command passed `9/9`, `0` failed/skipped. It verifies Event remains type 4, hidden locked nodes are rejected, dead ends lose even against higher rewards, server predecessor IDs determine the frontier, newly unlocked nodes require a new snapshot, unknown/default pauses, a worst-case terminal budget reaching 100 rejects, and floor index/ID tie-break deterministically.
- Full suite: Docker `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --configuration Release --nologo` passed `217/217`, `0` failed/skipped. Warning delta: none; no new warning category/location.
- Design notes: adjacency is built only from `PreviousFloorIds`; all predecessor IDs must exist, current floor must be present, and at least one Boss terminal must be reverse-reachable. A visible hidden node is eligible only when `IsUnlocked`; no local map/master next-edge is invented. Ranking is a declared comparison tuple, not an aggregate score.
- Concern: current pure model treats each Boss in the server-returned segment as a terminal; runtime mapping must supply only the active segment map so old/future segments cannot contaminate reachability.

## Task 3 — dynamic erosion projection and Safe-first code selection

- Base/head: `bc53c9e` → `b650b4e58e5116f020035d788fd05fb150df2bfc` (`feat: add nether erosion and code policies`).
- Precisely staged paths: `AbyssMod/Services/NetherErosionPolicy.cs`; `AbyssMod/Services/NetherCodePolicy.cs`; `AbyssMod.Tests/NetherErosionPolicyTests.cs`; `AbyssMod.Tests/NetherCodePolicyTests.cs`; `AbyssMod.Tests/AbyssMod.Tests.csproj`.
- Initial RED: Docker filtered `NetherErosionPolicyTests|NetherCodePolicyTests` exited `1` with `CS0246` for absent `NetherCodePortfolio`, `NetherCodeCandidate`, and `NetherCodeDecision` (and the erosion contract was likewise absent). This showed policy behavior was missing.
- Additional RED for the numeric-overflow safety mutation: after adding `Overflowed_code_levels_pause_instead_of_selecting`, Docker `--filter FullyQualifiedName~NetherCodePolicyTests` failed exactly with `Expected: Pause; Actual: Select`. The green change makes overflowed effective levels fail closed with `UnknownEffect` rather than selecting a code.
- GREEN focused: Docker filtered `NetherErosionPolicyTests|NetherCodePolicyTests` passed `21/21`, `0` failed/skipped. Coverage includes 0/5/10 deltas, soft 90/hard 100, mandatory Boss allowance, three-effect aggregation, unknown effects, drift/rebaseline, `30024`, Safe/Risk and Rush/Impact cancellation, Risk rejection/replacement, Safe5 protection, lane lock, reload reserve, incomplete data, and overflow pause.
- Full suite: Docker `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --configuration Release --nologo` passed `238/238`, `0` failed/skipped. Warning delta: none.
- Design notes: all additions are applied before declared rate modifiers; modifiers without a proven order or known semantics pause. The runtime mapper must only set `OrderKnown=true` after its current-version master/client order is confirmed. Code ranking is deterministic and never selects Risk/`40024`; on a full portfolio it removes Risk, research-only, off-lane, zero-coverage, then low rarity/coverage in order.
- Concern: rate semantics are represented as permille factors and deliberately require native/master confirmation in Task 6; unproven source effects remain fail-closed.

## Task 4 — generic event, recovery, treasure, and shop policy

- Base/head: `b650b4e` → `1bf7d22ac80ebea18412df5adc6c2ca421f9599f` (`feat: add safe nether node action policy`).
- Precisely staged paths: `AbyssMod/Services/NetherEventPolicy.cs`; `AbyssMod.Tests/NetherEventPolicyTests.cs`; `AbyssMod.Tests/AbyssMod.Tests.csproj`.
- RED: Docker `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --configuration Release --filter FullyQualifiedName~NetherEventPolicyTests --nologo` exited `1` with `CS0246` for absent `NetherEventPolicy` and `NetherEventOption`, proving the behavior contract was not pre-existing.
- GREEN focused: same Docker command passed `13/13`, `0` failed/skipped. Tests use literal effect fixtures for three-effect aggregation, lethal HP, hard erosion, erosion/HP/code ordering, unknown data, delayed event-battle classification, exact one-key treasure, unsafe treasure payment rejection, Shop Off, EquipmentBags eligibility, and Recovery.
- Full suite: Docker `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --configuration Release --nologo` passed `251/251`, `0` failed/skipped. Warning delta: none.
- Design notes: an event remains a `SelectEventOption` action even if its chosen result may start a battle. Effects are hard-validated at one to three entries, all unknown target/content or ambiguous code replacement inputs fail closed, and resource/HP/erosion checks happen before ranking. Shop Off can only produce `Leave`; buying requires a known type-91, Gold-or-better item paid from enough Nether Gold.
- Runtime boundary: this pure `Leave` result is not a raw `update-shop` call. Task 6 must prove the native shop close binding; if absent the bridge returns `BindingUnavailable` and coordinator pauses.

## Task 5 — Sleep, MaxDepth, ticket, Result, and return-item policies

- Base/head: `1bf7d22` → `17df2e49896aee6827e9afdb27443fb477be156d` (`feat: add nether checkpoint resource policies`).
- Precisely staged paths: `AbyssMod/Services/NetherCheckpointPolicy.cs`; `AbyssMod/Services/NetherReturnItemPolicy.cs`; `AbyssMod.Tests/NetherCheckpointPolicyTests.cs`; `AbyssMod.Tests/NetherReturnItemPolicyTests.cs`; `AbyssMod.Tests/AbyssMod.Tests.csproj`.
- RED: Docker filtered `NetherCheckpointPolicyTests|NetherReturnItemPolicyTests` exited `1` with `CS0246` for absent `NetherCheckpointDecision` and `NetherReturnItemSelection`, proving both policy boundaries were missing.
- GREEN focused: same Docker command passed `15/15`, `0` failed/skipped. Literal fixtures cover min(config/server/master) depth, no entry over the target, non-Sleep target pause, exactly-one-ticket Continue, Sleep finish at target/no ticket, Clear awaiting Result, Lose without Signal, preservation ID/type-91/drop/master rarity ordering, capacity, whole stacks, and missing master pause.
- Full suite: Docker `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --configuration Release --nologo` passed `266/266`, `0` failed/skipped. Warning delta: none.
- Design notes: `Sleep` is the sole Continue signal. Continue emits exactly `TicketCount=1`; target/no-ticket emits native-finish intent, while a target reached outside Sleep pauses without Cancel or Result. Return selection is entry-count bounded, never splits `Amount`, and validates every positive-lock item master before exposing a payload.
- Runtime boundary: Finish/Continue/return selection must still be driven through verified native popup/controller callbacks in Task 6; these policy results do not issue endpoints.

## Task 6 — current native binding audit (in progress)

- Uncommitted Task 6 work was inspected before further mutation: only `AbyssMod/Services/NetherAutoClimbModels.cs` and `AbyssMod.Tests/NetherAutoClimbStateMachineTests.cs` are modified by this task. The pre-existing modified `README.md` and untracked `build/` remain untouched. No runtime bridge, Harmony patch, generated artifact, or game-directory file has been created.
- TDD binding-selector RED: after the disposable SDK container restored the test project, `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --filter FullyQualifiedName~NetherAutoClimbStateMachineTests` exited `1` with `CS0246` for absent `NetherNativeMethodDescriptor`, `NetherNativeBindingSelection`, `NetherNativeMethodBindingSelector`, and `NetherNativeActionResultKind`. GREEN after the minimal pure selector implementation passed `11/11`, `0` failed/skipped; only the baseline nullable warnings appeared.
- Exact floor/recovery/battle/result evidence from the current RO game interop and ISIL dump: `Project.Nether.FloorSelection.SubViewController.HandleStartEventByStatusAsync(bool): UniTask`; private `OnFloorClickedEventAsync(int floorLevel, int floorIndex): UniTask`; private `ExecuteNextFloorMovementSequenceAsync(NetherFloorModel): UniTask<bool>`; `Project.Ingame.Exploration.NetherAPIService.StartQuestAsync(CancellationToken)`, `ClearQuestAsync(ExplorationBattleEndRecord, CancellationToken, bool)`, and `CloseQuestAsync(ExplorationBattleEndRecord, CancellationToken)`; and `Project.NetherTop.Result.SubViewController.CreateNetherResultModelAsync(bool, Il2CppReferenceArray<NetherResultPartyCharacterModel>)`, whose state machine calls `NetherApiDataStore.RequestNetherResultAsync`.
- Exact popup mutation evidence: Event/Recovery expose private `OnPanelSelected(popup, int)` plus `ExecuteEvent(popup)`; Treasure exposes `OnPanelSelected(popup, int)` plus `OnConfirm(popup)`; `AbyssCodeListPopupController` exposes `OnChangeTab(int)`, `OnClickThumbnail(int)`, `OnClickChange()`, and `OnClickReplace()`; `NetherReturnItemSelectionPopupController` exposes `OnThumbnailSelected(popup)`, `OnConfirmAsync(popup)`, and `OnCancelAsync(popup)`; Shop exposes `OnPurchaseContentAsync(popup, int)` and the native `RequestNetherUpdateShopAsync(master, ct)` only through that controller flow.
- Continue is uniquely bindable without raw requests, but only through exact current-version popup callbacks. `NetherContinueConfirmPopupController.<SetupPopupEvent>b__8_2(Unit, controller)` emits the native `(true, 1)` result when `_canBoost == false`. When `_canBoost == true`, it opens `NetherUtility.OpenNetherBoostConfirmPopupAsync`; the exact `NetherBoostConfirmPopupController.<SetupPopupEvent>b__7_2(int, controller, popup)` sets the consume count and `b__7_1(Unit, controller, popup)` invokes the original confirm callback. The captured result callbacks in `SubViewController.<>c__DisplayClass75_0.<OpenNetherContinueConfirmPopupAsync>b__0` and `NetherUtility.<>c__DisplayClass7_0.<OpenNetherBoostConfirmPopupAsync>b__0` call the native controller close path after setting the result. Therefore the bridge must use an exact signature selector for these compiler-generated symbols, fail closed on a zero/multiple match, and never mutate `_canBoost` or invoke `RequestNetherContinue` directly.
- Shop Off is uniquely bindable by capturing the original `Action close` argument at `NetherShopPopupController.SetupPopupEvent(NetherShopPopup, Action)`. ISIL shows its cancel subscription invokes `NetherShopPopupController.<>c.<SetupPopupEvent>b__16_0(Unit, Action)`, which invokes that original delegate. `SimplePopupControllerBase<T>.OpenPopupAsync` creates the delegate from its native `ClosePopup` path. The bridge must invoke only the captured delegate; `PopupBase.Close()` alone is not equivalent because it only begins visual close and does not emit the setup callback.
- Binding decision: **not blocked at this audit boundary**. Every required mutation has an identified original-controller flow, including the previously uncertain one-ticket Continue and Shop Off leave path. Runtime code must still gate every reflection call by exact name, arity, parameter type names, and return type, and must return `BindingUnavailable` rather than falling back to raw endpoints.

### Task 6 status — NEEDS_CONTEXT (implementation deliberately not started)

The requested stop point is **NEEDS_CONTEXT**, not DONE: no runtime bridge or Harmony lifecycle patch has been written, and the required native Code Reload mutation is not uniquely identified. It is not yet marked BLOCKED because the current targeted evidence does not prove that the game lacks a controller flow; it proves only that the exact controller/popup callback must be supplied or independently re-confirmed before implementation. No raw `NetherApiDataStore` request may substitute for it.

| Required action | Confirmed current-version native flow | Still missing / validation boundary |
|---|---|---|
| Floor select / restore | `Project.Nether.FloorSelection.SubViewController.OnFloorClickedEventAsync(int floorLevel, int floorIndex): UniTask` is the private native click chain; `ExecuteNextFloorMovementSequenceAsync(NetherFloorModel): UniTask<bool>` and `HandleStartEventByStatusAsync(bool): UniTask` are present. | Bridge/patch implementation and a live controller registration test only; no missing selector symbol. |
| Event option | `NetherEventPopupController.OnPanelSelected(NetherEventPopup,int)` + `ExecuteEvent(NetherEventPopup)`; Recovery has the same pair; Treasure has `OnPanelSelected(NetherTreasurePopup,int)` + `OnConfirm(NetherTreasurePopup)`. | `MNetherFloorEventParts.target_type_*` numeric-to-policy-effect mapping has not been confirmed, so unknown event effects must remain fail-closed. |
| Shop leave / purchase | Purchase: `NetherShopPopupController.OnPurchaseContentAsync(NetherShopPopup,int): UniTask` (with native confirmation flow), never its raw `RequestNetherUpdateShopAsync`. Leave: `SetupPopupEvent(NetherShopPopup, Action close)` subscribes `PopupBase.OnCancelAsObservable` to `<>c.<SetupPopupEvent>b__16_0(Unit,Action)`, which invokes the original `close` delegate; `SimplePopupControllerBase<T>.OpenPopupAsync` creates that delegate from its native `ClosePopup` path. | A Harmony prefix must capture the exact original `Action close` and map `MNetherFloorShopContents` to its displayed index. `PopupBase.Close()` is not an equivalent substitute. |
| Code select / reload | Select/replace: `AbyssCodeListPopupController.OnChangeTab(int)`, `OnClickThumbnail(int)`, `OnClickChange()`, and `OnClickReplace()` are present. | **Reload is unconfirmed.** `NetherDataStore.Apply(NetherReloadCodeResponseEntity)` is a post-response data-store update, not a mutation controller. No exact reload popup/controller callback was identified; invoking a raw reload endpoint would violate the design. |
| Return item | `NetherReturnItemSelectionPopupController.OnThumbnailSelected(NetherReturnItemSelectionPopup)`, `OnConfirmAsync(NetherReturnItemSelectionPopup): UniTask`, and `OnCancelAsync(...)` are present; the associated returnable-item scroll controller exposes the native indexed thumbnail selection path. | Exact mapping from selected server `NetherRewardItem` entries to current `ContentModel` indices still needs an implementation-time signature check. |
| Continue / finish at Sleep | `NetherContinueConfirmPopupController.<>c.<SetupPopupEvent>b__8_1(Unit,controller)` is the native return/finish callback. `b__8_2(Unit,controller)` gives native `(continue=true,useTicket=1)` when `_canBoost` is false; otherwise it opens `NetherUtility.OpenNetherBoostConfirmPopupAsync`. Its popup exposes `NetherBoostConfirmPopupController.<>c.<SetupPopupEvent>b__7_2(int,controller,popup)` to set one ticket and `b__7_1(Unit,controller,popup)` to confirm. The original display callbacks close the native controller after receiving the tuple/count. | These are compiler-generated symbols and must be exact-signature selected/captured in a bridge; no field mutation or raw `RequestNetherContinue` is allowed. |
| Battle / result | Observe only: `NetherAPIService.StartQuestAsync(CancellationToken)`, `ClearQuestAsync(ExplorationBattleEndRecord,CancellationToken,bool)`, and `CloseQuestAsync(ExplorationBattleEndRecord,CancellationToken)`; Result uses `Project.NetherTop.Result.SubViewController.CreateNetherResultModelAsync(bool,Il2CppReferenceArray<NetherResultPartyCharacterModel>)`, whose state machine calls `NetherApiDataStore.RequestNetherResultAsync`. | A result-completion wrapper must be compiled and verified; no missing observation symbol. |

Current Task 6 uncommitted work is intentionally limited to two pure, safe files:

- `AbyssMod/Services/NetherAutoClimbModels.cs`: adds structured native action outcomes (`Started`, `Completed`, `Rejected`, `UnknownOutcome`, `BindingUnavailable`) and an exact full-signature descriptor selector. It does not bind, invoke, or request anything.
- `AbyssMod.Tests/NetherAutoClimbStateMachineTests.cs`: adds two characterization tests for exactly-one matching name/arity/parameter-return signature and fail-closed zero/ambiguous matches. They passed `11/11` in the focused suite after the recorded RED.

No `NetherRuntimeBridge.cs`, `NetherAutoClimbPatch.cs`, PatchManager change, DLL, generated artifact, README change, build-directory change, or game-directory mutation exists. Do not commit the partial Task 6 selector while the required reload context is unresolved.

## Task 6 — reverse-confirmed IL2CPP runtime bridge and lifecycle hooks

- Base/head: `17df2e4` → `2f2f320 feat: bridge nether native runtime flow`.
- Precisely staged paths: `AbyssMod/Services/NetherRuntimeBridge.cs`; `AbyssMod/Patches/NetherAutoClimbPatch.cs`; `AbyssMod/Patches/PatchManager.cs`; `AbyssMod/Services/NetherAutoClimbModels.cs`; `AbyssMod.Tests/NetherAutoClimbStateMachineTests.cs`. `README.md`, `build/`, and `release/` are excluded.
- TDD RED/GREEN retained from the pre-existing Task 6 selector work: the focused test initially failed with missing `NetherNativeMethodDescriptor`/selector/result types; the minimal selector implementation then passed `11/11`. The bridge consumes that same pure exact-name/arity/parameter-return matcher; a zero or ambiguous reflection match produces `BindingUnavailable` before an invocation.
- Additional RO evidence closing the prior context gap: `Project.Nether.AbyssCodeSelectPopup.AbyssCodeSelectPopupController.RerollAsync(Project.Nether.AbyssCodeSelectPopup.AbyssCodeSelectPopup): UniTask`; its `<>c.<SetupPopupEvent>b__12_1(Unit,controller,popup)` calls the native state machine, and `<RerollAsync>d__22` invokes `NetherApiDataStore.RequestNetherReloadCodeAsync` internally. The bridge invokes only `RerollAsync`, never that datastore method.
- Implemented bridge bindings: Floor reconcile/select use `HandleStartEventByStatusAsync(bool)` / `OnFloorClickedEventAsync(int,int)`; Event/Recovery/Treasure use native select+execute/confirm pairs; Shop Off invokes only the captured original `SetupPopupEvent` close delegate and Shop purchase invokes `OnPurchaseContentAsync`; Code select uses the list controller click chain and reload uses `RerollAsync`; Return selection uses `NetherReturnableItemScrollViewController.OnThumbnailClicked(int)` followed by `OnConfirmAsync`; Continue/Finish invoke the exact compiler-generated `<>c` callbacks and boost one-ticket confirmation uses `b__7_2` then `b__7_1`; battle hooks are observation-only; Result completion is polled from the original Result `UniTask`.
- Snapshot mapping reads the registered `NetherModel`, `UserData.NetherDataStore`, and master caches on every capture. Missing map/party/master/content data returns a named mapping error rather than retaining prior authority. Known code IDs `30024` and `40024` and native erosion effect types are mapped; unproven ability-derived Safe/Risk/Rush/Impact rows remain `IsKnown=false` and therefore pause policy selection.
- First bridge compilation RED: initial RO build surfaced only internal compile defects (`out int` vs long reflection conversion, short-circuit `out` initialization, and IL2CPP enumerable iteration); all were corrected without changing a binding or adding a request fallback. A fresh `--no-restore` container lacks its targeting pack (`NETSDK1127`), so the required verification used restore+build in the same disposable container lifecycle.
- GREEN focused: Docker `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --configuration Release --filter FullyQualifiedName~NetherAutoClimbStateMachineTests --nologo` passed `11/11`, `0` failed/skipped. Only the 18 pre-existing nullable warning locations appeared.
- Full suite: Docker `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --configuration Release --nologo` passed `268/268`, `0` failed/skipped; warning delta none.
- RO plugin build: Docker `dotnet restore AbyssMod/AbyssMod.csproj -p:GameDir=/game && dotnet build AbyssMod/AbyssMod.csproj --configuration Release --no-restore -p:GameDir=/game -p:OutputPath=/work/release/nether-auto-climb/` completed `0` warnings / `0` errors and produced `release/nether-auto-climb/AbyssMod.dll`. The game mount was read-only and no output was written below `/game`.
- Runtime-only boundaries: static compilation cannot prove Unity popup timing or the server's numeric `MNetherFloorEventParts.target_type_*` semantics. The bridge therefore does not synthesize an event effect from unknown numeric targets, and any controller registration/index/content mapping mismatch fails closed; no raw `update`, `update-event`, `update-shop`, reload, continue, result, or cancel request is emitted by F12 code.

## Task 7 — recoverable F12 battle-settings lease and F11 coordination

- Base/head: `2f2f320` → `a54d911 feat: coordinate nether battle automation`.
- Precisely staged paths: `AbyssMod/Services/NetherBattleSettingsLeaseState.cs`; `AbyssMod/Services/NetherBattleSettingsLease.cs`; `AbyssMod/Services/BattleSessionAutoSL.cs`; `AbyssMod.Tests/NetherBattleSettingsLeaseStateTests.cs`; `AbyssMod.Tests/AbyssMod.Tests.csproj`. `README.md`, `build/`, and `release/` remain excluded.
- RED: Docker `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --filter FullyQualifiedName~NetherBattleSettingsLeaseStateTests --no-restore` initially failed with `CS0246` for the absent `NetherBattleSettingsLeaseState` contract. The first fresh disposable test container also showed missing NuGet analyzer metadata under `--no-restore`; restoring the test project in the same container resolved that environmental cache absence before the test result was evaluated.
- GREEN focused: Docker restore followed by the focused test passed `7/7`, `0` failed/skipped. It covers original-value capture before force, exact restoration, F12-disable restoration, unload restoration, persisted active recovery, atomic-save fault, and retryable restore fault.
- Full suite: Docker `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --configuration Release --no-restore --nologo` passed `275/275`, `0` failed/skipped. `git diff --check` passed. Warning delta: none; the focused non-incremental run reported only the pre-existing nullable warnings.
- RO plugin build: Docker restore+Release build against the read-only game mount completed `0` warnings / `0` errors and refreshed only `release/nether-auto-climb/AbyssMod.dll`.
- Design notes: the lease writes its saved original Auto/speed pair before a force transition, keeps its JSON lease until an observed exact restore, retries a failed restore, and exposes `HasActiveNetherOperation` so F11 does not race F12-owned battle handling.
- Runtime safety boundary: no exact current-version native Auto/highest-speed controller accessor was provided in the narrowed evidence set. `INetherBattleSettingsNative` is therefore deliberately unregistered unless a version-confirmed bridge supplies it; `AcquireAndForce` returns `BindingUnavailable("native-battle-settings-accessor-unregistered")`, and the future coordinator must pause rather than guess a property/setter, change a setting, or issue an API call. This is fail-closed, but a live F12 force/restore cycle still needs that exact controller binding before it can operate.

## Task 8 — F12 coordinator, configuration, lifecycle, and final artifact

- Base/head: `a54d911` → `5cd390e feat: add F12 nether auto-climb`.
- Precisely staged paths: `AbyssMod/Services/NetherAutoClimbController.cs`; `AbyssMod/Services/NetherAutoClimbModels.cs`; `AbyssMod/Services/NetherAutoClimbStateMachine.cs`; `AbyssMod/Services/NetherRuntimeBridge.cs`; `AbyssMod/Patches/NetherAutoClimbPatch.cs`; `AbyssMod/Core/Config.cs`; `AbyssMod/Core/Hotkey.cs`; `AbyssMod/Core/Plugin.cs`; `AbyssMod.Tests/NetherAutoClimbStateMachineTests.cs`; `AbyssMod.Tests/NetherCheckpointPolicyTests.cs`. `README.md`, `build/`, generated `release/`, and game files are excluded.
- Initial RED: after adding the four planned integration characterizations, Docker focused `NetherAutoClimbStateMachineTests` failed with `CS0246` for absent `NetherAutoClimbSettingsSnapshotGate`. This proves that invalid live config, stable-boundary reload latching, lowered MaxDepth, and disabled unknown-outcome reconciliation did not have the required integration contract. The minimal gate then made the focused suite pass `15/15`.
- Additional safety RED/GREEN: a zero SoftErosionLimit characterization first failed `Assert.False` because `0` was incorrectly accepted; the validator now rejects it as `invalid-soft-erosion-limit` and requires `1..99`. A positive `MaxDepth=500` characterization then failed because the validator incorrectly capped MaxDepth at 130; it now accepts every positive configured value and `NetherCheckpointPolicy` later applies the server/master minimum. Final focused state/checkpoint command passed `25/25`, `0` failed/skipped.
- Coordinator behavior: `Hotkey.Update` runs the controller only on Unity's main thread. It checks a registered live Nether controller, captures one settings snapshot only in `Stable`, begins exactly one native bridge action, polls the returned native `UniTask`, and reconciles only after completion/unknown outcome. A same fingerprint after a mutation is ambiguous and pauses; it is never treated as an idempotent retry. F12 off preserves an already pending reconciliation but schedules no new action. `BattleSessionAutoSL.HasActiveNetherOperation` moves Nether battle waiting into `AwaitingF11` without toggling or changing F11's separate config.
- Native lifecycle enhancement: `NetherRuntimeBridge` now retains the `UniTask` returned by exact controller methods (including return-item confirmation) and exposes its status through `PollNativeFlow`; this avoids treating an invocation as completed before the original flow has completed. The Result Harmony postfix remains observation-only and now protects the game callback with typed error logging; no postfix invokes a server action.
- Config: `[NetherAutoClimb]` binds `MaxDepth=130`, `SoftErosionLimit=90`, `MinimumCharacterHpPermille=300`, `CombatLane=Auto`, `CodeReloadReserve=1`, `TreasureMode=KeyOnly`, `ShopMode=Off`, and `DetailedLogging=true`. Descriptions include `200002`, `200001`, `30024`, `40024`, cancellation semantics, 90/100 limits, one-ticket Continue, and F11/F12 independence. The gate validates every non-boolean setting and applies an auto-reload only at the next `Stable` decision boundary.
- Checkpoint/return integration: Sleep Continue uses the exact one-ticket native path; target/no ticket uses native finish. Before Continue, the existing `NetherPreserveItemIds` parser is re-used (not a copied F12 setting); invalid parsing, master absence, or item-index mapping stops before Continue. Entire selected entries are sent to the already verified native return popup flow.
- Final verification: resolved `/work/release/nether-auto-climb` exactly, deleted only that prior ignored build output, then in one disposable Docker lifecycle restored and ran Release tests `281/281`, `0` failed/skipped, restored/built against `/game:ro` with `0` warnings / `0` errors, and verified `/work/release/nether-auto-climb/AbyssMod.dll`. No game file was written.
- Placeholder/forbidden-call self-scan: added Task 8 source has no `TODO`, `NotImplementedException`, empty catch, or `api/nether/cancel`; a full source scan also found no raw `api/nether/cancel` call. `git diff --check` passed.

### Task 8 self-review — design sections 1–17

| Spec section | Evidence / disposition |
|---|---|
| 1. Goal | `NetherAutoClimbController.cs`, F12 integration, and the policy/state commits implement the coordinator shape; live climbing deliberately waits for the two explicit first-real-run bindings below. |
| 2. Out of scope | No direct game API client, no game-directory deployment, and no modification of the independent F11 feature behavior. |
| 3. Global constraints | `NetherRuntimeBridge.cs` invokes only exact native controller flows; coordinator emits one action, waits/polls/reconciles, and fails closed. |
| 4. Authority/resources | Task 6 live snapshot mapper reads registered model/user/master state every capture; Task 8 consumes it without local floor/coin/ticket mutation. |
| 5. F12 lifecycle | `Toggle`, `Update`, `OnPluginUnload`, and plugin initialization/recovery implement on/off, registration checks, pause, completion, and lease restore. |
| 6. Single-flight reconciliation | `NetherAutoClimbStateMachine.cs` plus controller `Reconcile` preserve pending action/fingerprint and pause ambiguous same-fingerprint outcomes. |
| 7. Map/routes | `NetherRoutePlanner.cs` fully covers pure graph proof. Runtime node effect projection is **not yet confirmed**, so Task 8 marks every unprojected node unknown and pauses rather than chooses a potentially erosive route. |
| 8. Erosion/HP | `NetherErosionPolicy.cs`/event policy tests cover projection/drift; live per-node/event effect mapping remains a fail-closed boundary, not a guessed delta. |
| 9. Code | `NetherCodePolicy.cs`, `TryGetCodeCandidates`, native select/reload, stable config snapshots, and controller lane lock are present; unknown code master/effect data pauses. |
| 10. Node actions | Event/Recovery/Treasure/Shop pure policies and Task 6 controller bindings exist. Current popup payload-to-policy mapping is unconfirmed, so coordinator pauses before selecting such a popup rather than invoke by stale/guessed index. |
| 11. Battle/F11/settings | Battle observation and F11 wait are implemented; lease persistence/retry exists. Exact native Auto/highest-speed accessor remains unprovided, so enable returns `BindingUnavailable` before changing any setting. |
| 12. MaxDepth/Sleep | `NetherCheckpointPolicy.cs` and controller handle effective cap, exact one ticket, native finish, and non-Sleep target pause. |
| 13. Return items | `NetherReturnItemPolicy.cs`, existing `NetherPreserveItemIds`, and native popup bridge select whole server entries with fail-closed index/master checks. |
| 14. Config/reload | `[NetherAutoClimb]` bindings and `NetherAutoClimbSettingsSnapshotGate` cover defaults, validation, and next-Stable reload semantics. |
| 15. Logging | `[F12][NetherClimb]` logs transitions/actions/pauses/lease failures without per-frame polling. Full event/route candidate and erosion prediction logs await the same unconfirmed popup/effect mappings. |
| 16. Safe pause/end | Binding, snapshot, config, master, route, result, and lease failures all stop new scheduling and attempt lease restore; no automatic Signal use is introduced. |
| 17. Acceptance | Pure suite `281/281`, focused Task 8 suite `25/25`, RO-game Release build `0/0`, artifact verified, diff/placeholder/raw-cancel scan clean. First-real-run boundaries are explicitly recorded. |

### Remaining first-real-run / needs-context boundaries

1. **Auto + highest-speed native setting accessor:** no exact current-version controller/property getter+setter flow was supplied. The lease remains intentionally unregistered, so F12 currently pauses at enable (`native-battle-settings-accessor-unregistered`) before changing any player setting.
2. **Per-floor/event/shop popup payload mapping:** the current bridge proves how to invoke native controllers but does not yet prove model fields/semantic mapping required to predict node erosion/HP or choose Event/Recovery/Treasure/Shop options. Route selection is therefore intentionally disabled by an explicit unknown-node context; it does not guess or issue raw requests.

These are functional readiness blockers, not compile/test blockers. No unsafe fallback was added.

## Review round 1 remediation — exact runtime seams and safety fixes

### Scope and native evidence

- Review findings were technically re-checked against the current RO game and ISIL/Diffable output before changing behavior. No raw Nether endpoint was added. All mutations remain an exact registered-controller method/callback; a zero/multiple reflection match remains `BindingUnavailable`.
- Battle settings: current game evidence is `Project.Ingame.IIngameUserSettings.get_IsAuto/set_IsAuto`, `get_Speed/set_Speed`, `Project.GameSpeedType.Triple = 3`, and `Project.Ingame.BottomRightView.ApplyUserSettings(IIngameUserSettings)`/`OnDestroy`. The bridge registers only that native settings instance, the lease is acquired only at confirmed Nether battle start, and it restores on clear/close, F12 off, pause, scene teardown, unload, and persisted recovery. `RetryRestore` is wired through the runtime lease wrapper.
- Read-only reconciliation: `Project.Nether.FloorSelection.SubViewController.CreateNetherModelAsync(): UniTask` is used because its generated ISIL state calls `NetherApiDataStore.RequestNetherAsync`; it is polled to terminal success rather than treating invocation return as server completion.
- Code select/replacement: the normal offer uses `AbyssCodeSelectPopupController.<>c.<SetupPopupEvent>b__12_3(int, controller, popup)` to select the native detail, then `b__12_0(Unit, controller)` to invoke its original Receive callback. The exact generated `Project.Nether.NetherUtility.<OpenAbyssCodeSelectPopupIfNeededAsync>g__HandleConfirmSequenceAsync|19_2(AbyssCodeSelectPopupController,long,NetherPartyModel,CancellationToken): UniTask` is Harmony-observed. ISIL proves that task awaits `OpenReplaceAbyssCodeListPopupAsync` and later calls `RequestNetherFixCodeAsync`; F12 waits for the task rather than a callback return.
- At capacity, only a fresh `AbyssCodeListPopupType.Replace` registration (sequence newer than the offer) is accepted. The native order is `OnChangeTab(int)` → `OnClickThumbnail(int)` → `OnClickReplace()`, then the original confirmation UniTask must succeed. A stale list, wrong popup type, task fault/cancel, missing task, or timeout fails closed and cannot repeat the mutation.
- Checkpoint evidence remains the exact instance-generated `<>c.<>9` callbacks: Continue `b__8_2`, Finish `b__8_1`, Boost count `b__7_2`, Boost confirm `b__7_1`. The staged bridge does not inspect or rank return candidates until native Continue/optional Boost has produced the pristine Return popup/scroll model; Finish never enters return selection.

### Implemented reviewer fixes

- F12 no longer forces Auto/Triple on enable. It drains an in-flight operation on off, rejects an attempted re-enable before the observation/reconcile drain completes, and schedules no new mutation while disabled.
- Live route mapping reads floor id/level/index/type/previous edges/hidden/unlocked state from `NetherFloorModel`; the route planner receives the effective MaxDepth candidate gate rather than marking every node unknown. Floor-service termination clears all bridge registrations and pending callback/task state.
- Wait dispatch distinguishes Code, Event, Recovery, Treasure, Shop, Continue/Sleep, and checkpoint contexts. Raw event type `4` remains Event. Recovery permits a neutral safe fallback. Popup registration only exposes the actual current popup sequence.
- Reconciliation uses action-specific fingerprint/graph/resource/type postconditions. Same-state and unknown outcomes are ambiguous, trigger read-only refresh first, and never resend a non-idempotent native action.
- Result task observation has a bounded registration wait and terminal failure is surfaced rather than remaining forever in `AwaitingSceneChange`. Erosion/HP action projection is calibrated against authoritative snapshots and gates 90/100 limits before mutation; candidate/action/snapshot logs are bounded and silent when DetailedLogging is off.
- Return mapping uses the displayed post-Continue `ContentModel` (`ContentId`, `Amount`, `ContentRarity`) plus master item data. It no longer fabricates an unverified `NoEffect` rarity/master mapping.

### TDD and verification evidence

- RED: `NetherCodeSelectionNativeFlowTests` first failed with missing `NetherCodeSelectionNativeFlow` / `NetherCodeSelectionNativeStage`. GREEN: `3/3`, covering confirmation-task wait, fresh replacement popup sequencing, direct-offer path, and stale/out-of-order rejection.
- RED: `NetherCodeDiagnosticAuditTests` first failed with missing `NetherCodeDiagnosticAudit` / `NetherCodeMasterAudit`. GREEN: `2/2`, proving detailed-off silence plus an eight-entry bounded, identifier-only audit payload.
- Combined focused Docker test command for state/lease wait/checkpoint/code paths passed `28/28`, `0` failed/skipped. It includes F12-off in-flight drain/no early re-enable, checkpoint Continue→Boost→pristine Return ordering, bounded native task registration waits, and code replacement sequencing.
- Final fresh Docker full suite: `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --logger 'console;verbosity=minimal'` passed `308/308`, `0` failed/skipped. The previous non-incremental nullable warnings are pre-existing in `TranslationPaths`, `Manifest`, and `StaticBundleProtocol`; the final incremental verification introduced no feature warning.
- Fresh RO-game Release build command: `ABYSS_GAME_DIR=/game dotnet build AbyssMod/AbyssMod.csproj -c Release -p:BaseOutputPath=/repo/release/nether-auto-climb/` completed `0` warnings / `0` errors and produced `release/nether-auto-climb/Release/net6.0/AbyssMod.dll`. `/game` and `/reverse` were mounted read-only; output stayed under the ignored repo `release/` directory.
- During this fix round, an initial build without `ABYSS_GAME_DIR=/game` correctly failed only because the disposable container could not resolve game interop references; it was not treated as source verification. A subsequent diagnostic build caught two local C# assignment/deconstruction mistakes in the new audit collection; both were corrected before the green RO build above.

### Code semantic mapping — NEEDS_CONTEXT

Current RO evidence is insufficient to classify ordinary code offers without guessing:

- `MNetherCodes` gives `id`, `category`, `effect_type`, `effect_parameter_1..3`, `asset_id`, `rarity`, and `power`.
- `NetherCodeAbilityAssetDataStore.GetAbilityEffectAsset(long)` returns `IAbilityEffectData` structural data (`ID`, scope, target, situations, effect type), while `AbyssCodeThumbnailModel` exposes only `NetherCodeCategoryType`.
- None of those verified types/methods defines the product semantics Safe/Risk/Rush/Impact, party coverage, or research-only. In particular, ability effect type/target class names cannot safely be inferred as the requested combat lane.

Accordingly, `30024`, `40024`, and proven erosion effect types remain mapped; every other unproven ordinary code remains `IsKnown=false` and F12 pauses before any selection/reload. This means common code offers may stop the run and is a functional readiness blocker, not a routine live-validation note. With DetailedLogging enabled, the bridge emits at most eight unknown rows containing only `code id`, master category/effect/parameters/rarity/power/asset identifier, and ability ID/scope/target/effect type identifiers (never code display name/description or unrelated player data).

Minimum missing context to close this safely: one live-game capture of that bounded audit for an ordinary Safe/Risk/Rush/Impact offer plus the corresponding authoritative semantic source (runtime model property, master mapping table, or native selector/effect enum mapping) that ties each listed code/ability identifier to Safe/Risk/Rush/Impact, party coverage, and research-only status. Without it, a guessed classifier would violate the Safe-first/Risk-hard-reject requirement.

### Post-commit final verification

- Commit: `f46bdd69f583ba822fbb3b576d42ae393661870b fix: harden nether auto-climb runtime flow` (33 precisely staged implementation/test files; no `README.md`, `build/`, release artifact, or game path staged).
- In a new `docker run --rm` with `/game:ro` and `/reverse:ro`, the combined final command ran `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --logger 'console;verbosity=minimal'`, `ABYSS_GAME_DIR=/game dotnet build AbyssMod/AbyssMod.csproj -c Release -p:BaseOutputPath=/repo/release/nether-auto-climb/`, `git diff --check`, `git diff --cached --check`, artifact existence check, and `git status --short`.
- Result: `308/308` passed, `0` failed/skipped. The full clean-container test emitted only the 18 established nullable warnings in `TranslationPaths.cs`, `Manifest.cs`, and `StaticBundleProtocol.cs`; the RO Release build emitted `0` warnings / `0` errors and produced `release/nether-auto-climb/Release/net6.0/AbyssMod.dll`.
- Both diff checks passed. Final working tree scope is exactly the preserved user changes: `M README.md` and `?? build/`.

## Fix round 2 — Milestone A: floor parent task and owned modal stages

### Scope and production wiring

- This milestone is intentionally limited to review-round-2 chain A. It does not change the route/reconcile/lease/checkpoint work reserved for Milestones B/C.
- `NetherAutoClimbController` now starts `NetherRuntimeFlowCoordinator` for `SelectFloor`, passes its generation to `NetherRuntimeBridge.BeginFloorParent`, and polls that same production coordinator while the parent is pending. The test seam is the small `INetherRuntimeParentDriver` adapter; the class under test is the production coordinator directly used by the controller, rather than a test-only helper.
- The bridge records a parent `UniTask` independently of intermediate popup callbacks. While it is pending, `TryGetOwnedPopup` and `InvokeOwnedPopup` require the exact parent action, owner generation, registration sequence, and popup object identity. The controller can dispatch one matching Event/Recovery/Treasure/Shop/Code child, then continues polling the original parent task; it cannot issue a second floor choice.
- Popup records now contain owner action, owner generation, sequence, and liveness. Exact `Project.PopupBase.Close`, `Project.PopupBase.ImmediatelyClose`, and `Absf.MonoBehaviourWithUniTask.OnDestroy` observations invalidate only the matching reference/sequence. Parent terminal cleanup removes only that parent generation, so an older close cannot erase a newer popup and `Play` cannot replay a stale modal.
- Event and Treasure inner `UniTask.Void` callbacks are explicitly not treated as proof of completion: the parent native task is the only terminal evidence in this stage.

### Native Code callback evidence

- The RO ISIL/Diffable evidence for `Project.Nether.AbyssCodeSelectPopup.AbyssCodeSelectPopupController.<>c.<SetupPopupEvent>` distinguishes `b__12_0` (cancel) from `b__12_2` (confirm). `NetherCodePopupNativeBinding.ConfirmDescriptor` binds only `<SetupPopupEvent>b__12_2(Unit, controller): void`; it does not use the visually adjacent cancel delegate. Detail selection remains `b__12_3`, and the existing observed confirmation/replacement parent task remains the terminal chain for capacity replacement.

### TDD evidence

- RED: before the runtime seam existed, the focused test project failed to compile the missing `INetherRuntimeParentDriver`/`NetherRuntimePopupResult` production symbols. Adding the tests before the production types made the intended integration dependency explicit.
- RED mutation: `Parent_terminal_is_not_consumed_on_the_same_tick_as_owned_modal_dispatch` failed with `Expected: Pending; Actual: Completed`. The coordinator was then changed to return pending on the dispatch tick and wait for the original parent task on the next poll. This catches the Event/Treasure void-inner premature-completion regression.
- GREEN focused command in a fresh `/game:ro` + `/reverse:ro` Docker container: `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --no-restore --filter 'FullyQualifiedName~NetherRuntimeFlowCoordinatorTests|FullyQualifiedName~NetherPopupOwnershipRegistryTests|FullyQualifiedName~NetherCodePopupNativeBindingTests'` passed `7/7`, `0` failed/skipped. Coverage includes parent pending/one-child dispatch, stale prior-generation suppression, same-tick terminal suppression, exact old-close/new-popup isolation, owner-scoped terminal cleanup, and the distinct confirm/cancel descriptor characterization.

### Milestone verification

- Fresh Docker full suite: `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --no-restore --logger 'console;verbosity=minimal'` passed `315/315`, `0` failed/skipped.
- Fresh RO-game build: with `ABYSS_GAME_DIR=/game`, `dotnet build AbyssMod/AbyssMod.csproj -c Release --no-restore -p:BaseOutputPath=/repo/release/nether-auto-climb/` completed with `0` warnings / `0` errors. It produced `release/nether-auto-climb/Release/net6.0/AbyssMod.dll`; `/game` and `/reverse` were read-only mounts.
- `git diff --check` passed before staging. The pending stage intentionally contains only the ten Milestone-A implementation/test files; the user-owned `README.md`, untracked `build/`, reports, and ignored Release artifact are excluded.
- Commit: `7f8245b fix: own nether floor modal flow` (10 precisely staged Milestone-A files; no user-owned path or game path was staged).
- Post-commit repeat verification in fresh Docker containers: full suite remained `315/315` passed, and the RO `/game:ro` Release build remained `0` warnings / `0` errors. `git diff --check` and cached diff check both passed; status remained only the preserved user `M README.md` and `?? build/`.

### Next milestone boundary

Milestone B can build on the now-stable `INetherRuntimeBridge` parent-task boundary to add GET-only reconcile, action-specific settlement/battle/result ownership, and Continue scene handoff. It must not weaken the generation/sequence/object-identity checks established here.

## Fix round 2 — Milestone B1: GET-only authority and battle settlement lifecycle

### Scope and exact native authority evidence

- This milestone implements only B1a/B1b: read-only Nether reconciliation, target-specific postconditions, and battle lifecycle drain. It does not alter B2 Continue scene handoff, route/erosion safety, or lease phase work.
- The native binding is deliberately `Project.User.NetherDataStore.SyncNetherDataAsync(CancellationToken): UniTask`, invoked only on the live `UserData.NetherDataStore` instance. RO evidence: `reverse_out/cpp2il_latest/DiffableCs/Project/Project/User/NetherDataStore.cs`; `reverse_out/cpp2il_isil/IsilDump/Project/Project/User/NetherDataStore_NestedType__SyncNetherDataAsync_d__37.txt` calls `RequestNetherAsyncInternal` at its state-machine call site; `...RequestNetherAsyncInternal_d__38.txt` uniquely calls `NetherApiDataStore.RequestNetherAsync` and then applies Nether/Item/LockItem/Sphere/Point/Code/Stamina stores. The bridge polls the returned native `UniTask` before capturing the snapshot.
- This avoids `FloorSelection.SubViewController.CreateNetherModelAsync`: its RO state machine contains a `RequestNetherStartAsync` branch for `NotPlayed`, so it is not a valid reconciliation fallback. No raw endpoint, Start/Resume/Continue fallback, or client-side datastore mutation was added.

### B1a target-specific reconciliation

- `NetherReadOnlyReconcileCoordinator` is production code used by `NetherAutoClimbController.Reconcile`, with a thin native `NetherReadOnlyReconcileNativeBinding`/runtime bridge adapter. It begins exactly one `SyncNetherDataAsync`, waits for native terminal success, then captures the applied authority. Missing live store or exact-binding failure becomes `BindingUnavailable`, never a synthetic API call.
- `NetherPlannedAction` now persists immutable pre-action fields for exact floor/status, code/replacement, shop content/cost, event effects, and Continue ticket/map/segment conditions. `NetherActionReconcilePolicy` returns `Applied` only when the action's own authority matches. Wrong floor, code, buy delta/cost, ticket, or map is `Ambiguous`; `NotApplied` is reserved for an authoritative unchanged/negative observation.
- RED: `NetherReadOnlyReconcileCoordinatorTests` first failed to compile before the production `INetherReadOnlyReconcileDriver`/snapshot contract existed; target-policy tests then failed because wrong floor/code/buy/ticket/map snapshots were incorrectly classified Applied. GREEN added the coordinator and strict postconditions. The test seam is the real coordinator called from the controller, not a parallel test-only policy helper.

### B1b explicit battle settlement and F12 drain

- `NetherActionKind.BattleSettlement` plus `NetherBattleSettlementContract` replaces the battle use of generic `AwaitNativeFlow`. `BeginBattleWait` records entry map/floor/status and an immutable projection identity; it starts no battle mutation. `NetherBattleSettlementCoordinator` observes the native clear/close parent terminal, then runs one GET-only reconciliation and accepts only the contract's exact map/floor/status result. Unchanged, wrong target, binding loss, fault, cancellation, and scene loss all become named terminal pauses.
- `INetherBattleSettlementDriver` is implemented by the production runtime bridge. F11 busy holds the contract in `AwaitingF11` without beginning GET; clear/close transitions it to `AwaitingBattleSettlement`; only a successful applied snapshot completes it. Native task completion alone is never settlement proof.
- State-machine drain now covers `ExecutingNativeAction`, `Reconciling`, `AwaitingF11`, `AwaitingBattle`, `AwaitingBattleSettlement`, and scene/result transition. F12-off keeps pending evidence and blocks an off→on repeat until terminal observation/reconciliation. Terminal battle faults clear both state evidence and the coordinator's retained contract, so a lost scene cannot replay stale work.
- RED: the new battle tests initially failed on the missing `INetherBattleSettlementDriver`; after the coordinator was added, exact/unchanged settlement tests still failed because `NetherActionReconcilePolicy` had no `BattleSettlement` branch. The explicit branch and contract made those cases GREEN. The additional off/re-enable tests cover AwaitingF11, AwaitingBattle, and AwaitingBattleSettlement rather than just an ordinary native action.

### Tests and verification

- Fresh Docker focused suite (after restoring the disposable container's NuGet cache): `NetherBattleSettlementCoordinatorTests`, `NetherReadOnlyReconcileCoordinatorTests`, `NetherActionReconcilePolicyTests`, and `NetherAutoClimbStateMachineTests` passed `39/39`, `0` failed/skipped. It covers NotPlayed/no-start driver behavior, GET terminal wait, exact/wrong action targets, battle success/unchanged/wrong/fault/cancel, F11 busy, scene loss, and F12 drain/re-enable prevention.
- The first fresh `--no-restore` test attempt failed before compilation because the disposable `/tmp/abyssmod-nuget` cache did not exist (`CS0006` xUnit analyzer metadata); it was an environment-cache failure, not a source result. Restore and test ran in the same subsequent disposable container. The final focused and full outputs had only the established nullable warnings from `TranslationPaths`, `Manifest`, and `StaticBundleProtocol` during non-incremental compilation.
- Fresh Docker full suite: `333/333` passed, `0` failed/skipped.
- Fresh RO build: `dotnet restore AbyssMod/AbyssMod.csproj -p:GameDir=/game` followed by `dotnet build ... -c Release --no-restore -p:GameDir=/game -p:BaseOutputPath=/repo/release/nether-auto-climb/` completed `0` warnings / `0` errors. A transient NuGet SSL retry succeeded during restore. The only output was `release/nether-auto-climb/Release/net6.0/AbyssMod.dll`; `/game` and `/reverse` were read-only.

### Next milestone interface

Milestone B2 may extend `NetherReadOnlyReconcileCoordinator` and the immutable `NetherPlannedAction` payloads for Continue's bounded scene-handoff/rebind and Result ownership. Milestone C may attach real route/erosion/HP projection and per-battle lease phase evidence. Neither may restore generic battle `AwaitNativeFlow`, treat a native task return as authority, or weaken the no-reenable drain.

### Commit and clean handoff

- Commit: `c9d2dee9ff4740f1621a4b37374f96c8b7581668 fix: reconcile nether battle settlement` (13 precisely staged B1 implementation/test files). `README.md`, `build/`, ignored reports, ignored Release output, and every game/reverse path were excluded.

## Fix round 2 — Milestone B2: Result/Continue/checkpoint lifecycle and carry-out preflight

### Scope and production lifecycle wiring

- This milestone closes only the B2 lifecycle chain. It does not broaden route/erosion/HP, code semantics, or battle-lease work reserved for C.
- `NetherResultSceneCoordinator` owns Result evidence independently from FloorSelection. `UnregisterFloorSelectionCore` now clears only floor-owned registrations; a Finish can tear down FloorSelection and still expose the Result parent as pending, succeeded, faulted, or canceled. Controller observation of `AwaitingSceneChange` no longer requires a registered FloorSelection.
- `NetherContinueSceneCoordinator` and the controller-facing `NetherContinueSceneRuntimeCoordinator` model the exact Continue sequence: captured old owner/generation and immutable pre-snapshot, native parent terminal, expected old FloorSelection teardown, newer NetherTop runtime registration, exactly one GET-only reconcile, then exact ticket `-1`, map/floor/segment/status settlement. Wrong scene/generation, missing rebind, timeout, parent fault/cancel, F12 off/re-enable, and Finish-vs-Continue separation have named terminal outcomes.
- `NetherCheckpointPopupWaitCoordinator` owns independent bounded gates for Continue, Boost, Return, and ReturnScroll. It binds every registration to action/owner generation/sequence/liveness and polls the captured `HandleStartEventByStatusAsync` parent concurrently. Parent fault/cancel, early terminal without the expected stage, stale/closed registration, and missing popup become terminal fail-closed results; a fresh popup registered on the bound tick is accepted. Finish owns only Continue and never enters Return/ReturnScroll.

### Carry-out preflight and exact native evidence

- New production component `NetherCheckpointReturnPreflight` makes the pre-mutation carry-out contract executable. Positive `LockReward` requires a complete authoritative descriptor for every whole acquired entry: item id, amount, `MItems.type`, `MItems.rarity`, and knowledge flags. It emits only `Ready` (selection limit, deterministic pristine SHA-256 fingerprint, and whole-entry selection), `NoReturn` (`LockReward=0`), or fail-closed `Pause`; unknown master/content/rarity, invalid entry, or a limit exceeding available whole entries cannot authorize the native parent.
- `NetherAutoClimbController.ExecuteContinueNativeAction` invokes `INetherRuntimeBridge.PreflightContinueReturn` before `State.TryBegin`, Continue scene handoff, or `Invoke`. Thus a paused preflight cannot create the native `HandleStartEventByStatusAsync` parent and cannot reach a Continue API chain. A valid decision is stored immutably in `NetherPlannedAction` as lock limit, expected pristine hash, and whole-entry selection.
- `NetherRuntimeBridge.PreflightContinueReturn` reads only the live `UserData.NetherDataStore`: `NetherPointData.LockReward`, `GetOtherAcquiredItemDataList()`, and the local `MasterDataStore.GetCache<MItems>()`. It makes no endpoint/raw request and no callback invocation. `LockReward=0` deliberately bypasses irrelevant item-master mapping and allows the native no-return branch.
- Exact RO evidence: `reverse_out/cpp2il_isil/IsilDump/Project/Project/Nether/NetherReturnItemSelectionPopup/NetherReturnItemSelectionPopupController_NestedType__InitializeViewAsync_d__7.txt` calls `NetherDataStore.GetOtherAcquiredItemDataList` then `NetherUtility.ToContentModelList`, and awaits `GetNetherPointDataAsync`; Diffable `Project/User/NetherPointData.cs` shows `LockReward` at field offset `0x3C`. `SubViewController_NestedType__HandleGameClearedIfNeededAsync_d__68.txt` reads that point value before `RequestNetherContinueAsync`, taking the return-popup path for positive reward count. `NetherReturnItemSelectionPopupController_NestedType__OnConfirmAsync_d__14.txt` is the exact `RequestNetherContinueAsync` chain.
- After fresh Return popup/scroll registration, `SelectCheckpointReturnItems` maps its pristine `ContentModel` list, reads the exact native `_maxSelectedCount` fields from both the Return controller and scroll controller, and compares their limits plus the content/master fingerprint and whole-entry selection to the immutable preflight. Any mismatch returns `BindingUnavailable` before native `OnConfirmAsync`; only the verified fresh popup can be selected/confirmed.

### Exact one-ticket / boost boundary

- The controller requires `TicketCount==1` and `TicketCost==1` before preflight. The only automatically invoked Continue callback remains the proven `NetherContinueConfirmPopupController.<>c.<SetupPopupEvent>b__8_2(Unit, controller)` when `_canBoost==false`, yielding the native one-ticket default.
- RO `NetherContinueConfirmPopupController_NestedType___c.txt` and `...DisplayClass8_0_NestedType___SetupPopupEvent_b__3_d.txt` prove that `b__8_2` opens `OpenNetherBoostConfirmPopupAsync` when `_canBoost==true`; `b__8_1` is the cancel/result-finish callback, not a non-boost Continue action. Therefore the runtime refuses `_canBoost==true` before any callback/request rather than guessing a replacement callback, changing ticket multiplier, or invalidating the preflight. This is the remaining live-only operational boundary: a live run with that flag pauses for user control until a verified native non-boost route is available.

### TDD and verification

- RED: `NetherCheckpointReturnPreflightTests` first failed with missing preflight types, then the added parent/confirm authorization tests failed with missing `CanStartNativeContinueParent`, `CanConfirmReturnPopup`, and `VerifyFreshPopup`. GREEN covers `LockReward=0`, positive ranking/preserve behavior, unknown master/content/rarity, limit overflow, whole-entry amount preservation, deterministic hash, no parent authorization before Ready, fresh popup success, and fresh limit/hash mismatch rejection.
- The production seam is used by both Controller and RuntimeBridge; it is not a test-only selector. B2 characterization tests cover Result teardown, Continue handoff success/wrong/timeout, F12 lifecycle, popup parent fault/cancel/early complete/late/stale behavior, and the preflight/confirm gates.
- Final fresh Docker focused aggregate command passed `83/83`, `0` failed/skipped: `NetherResultSceneCoordinatorTests`, `NetherContinueSceneCoordinatorTests`, `NetherContinueSceneRuntimeCoordinatorTests`, `NetherCheckpointNativeFlowTests`, `NetherCheckpointPopupWaitCoordinatorTests`, `NetherCheckpointReturnPreflightTests`, `NetherCheckpointPolicyTests`, and `NetherAutoClimbStateMachineTests`.
- Final fresh Docker full suite passed `383/383`, `0` failed/skipped. Fresh RO build used `/game:ro` and `/reverse:ro`, restored against `GameDir=/game`, produced `release/nether-auto-climb/Release/net6.0/AbyssMod.dll`, and completed with `0` warnings / `0` errors.
- Scope rule: the B2 logical commit stages only B2 implementation/test files plus this ledger; preserved user `README.md`, untracked `build/`, Release output, and game/reverse paths are excluded.

## Fix round 2 — Milestone C1: route safety, battle calibration, and interactive pre-entry

### Scope and production wiring

- C1 replaces the previous permissive combat-route inputs with a complete fail-closed production chain: exact per-floor `MNetherMapFloors.min_erosion_point/max_erosion_point` bounds, live FloorSelection party `HpRatio` permille, active possession-code effect projection/hash, `NetherErosionPolicy.ProjectBattle`, `NetherFloorSafetyEvaluator`, reverse terminal worst-case context, and the existing production route coordinator/wiring used by `NetherAutoClimbController.PlanRoute`.
- `NetherBattleProjectionCalibration` is applied after the authoritative BattleSettlement GET snapshot. It requires the immutable entry map/floor/code-hash/projection identity, rejects unknown/decreasing/overflowing erosion or an actual delta outside the projected range, and only then rebaselines the next route decision.
- `NetherRuntimeBridge.TryCaptureInteractivePreEntryInputs` reads the live `NetherFloorModel` exact `MNetherMapFloorId`, `ExtendId`, and `FloorType`, plus raw MNetherMapFloors/MNetherFloorEvents/MNetherFloorEventParts fields and snapshot resources. It checks duplicate/reference integrity and passes the copied rows into `NetherInteractiveFloorPreEntrySafety`; missing or malformed data is never converted to zero/true.
- Controller `PlanRoute` now captures that all-or-nothing interactive result and passes it to `NetherAutoClimbRouteSafetyWiring`/`NetherRouteSafetyProductionCoordinator`. Event, Recovery, Shop, and Treasure receive a complete evaluator input only when the capture matches the current snapshot/settings and the pre-entry evaluator has already proved a safe exit for every possible weighted row. Otherwise the node is explicitly unknown. Shop Off requires the exact RO-confirmed `SetupPopupEvent(NetherShopPopup, Action)` close descriptor; key-only Treasure requires an authoritative key.

### TDD and characterization coverage

- RED C1d2b: the new `NetherInteractiveRouteSafetyWiringTests` first failed with `CS1739` because the production `NetherAutoClimbRouteSafetyWiring.Plan` surface did not accept the interactive pre-entry result. GREEN added the all-or-nothing argument and coordinator consumption. The tests execute raw reflection-shaped capture → pre-entry evaluator → production route coordinator/wiring, rather than a standalone policy helper.
- The production route seam covers floor bounds/duplicate/invalid range, party minimum HP, code effects 6–9/hash (ordinary effects retained in hash), 0/5/10 ranges, soft 90/hard 100, necessary Boss exceptions, reverse terminal cost, unknown map/code/HP input, post-battle drift, and MaxDepth.
- Interactive wiring coverage proves all-positive-row Event selection, a single unsafe possible Event row rejection, neutral Recovery, Shop Off close-binding requirement, Treasure key/no-key behavior, missing master rejection, and a mutation proof that an otherwise plausible raw capture cannot bypass a rejected `NetherInteractiveFloorPreEntrySafety` result.

### Fresh final verification

- One fresh `docker run --rm` with `/game:ro` and `/reverse:ro` ran the C1 safety aggregate: `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --filter 'FullyQualifiedName~NetherFloorSafetyEvaluatorTests|…|FullyQualifiedName~NetherInteractiveRouteSafetyWiringTests'`. Result: `111/111` passed, `0` failed/skipped.
- The same fresh container then ran the complete test project: `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj`. Result: `485/485` passed, `0` failed/skipped.
- RO Release validation: `ABYSS_GAME_DIR=/game dotnet build AbyssMod/AbyssMod.csproj -c Release -p:BaseOutputPath=/repo/release/nether-auto-climb/` produced `release/nether-auto-climb/Release/net6.0/AbyssMod.dll` with `0` warnings and `0` errors. Restore retried once after a transient package-source SSL EOF, then completed; this did not change source/build results.
- `git diff --check` passed before staging. `README.md`, untracked `build/`, ignored report/ledger paths, Release output, and the RO game/reverse mounts are excluded from the C1 commit.
- Commit: `477a073 fix: wire nether route safety authority` (33 precisely staged C1 implementation/test files). Post-commit status contains only preserved user `M README.md` and `?? build/`.

## Fix round 2 — Milestone C2b: battle settings lease lifecycle

### Scope and production wiring

- Added the production `NetherBattleSettingsLeaseControllerLifecycle` seam around the existing persisted `NetherBattleSettingsLease` and `NetherBattleSettingsLeaseRuntimeCoordinator`. It owns no copied Auto/speed or session-global restore bit: `NetherBattleSettingsLease.Phase` remains authoritative across every battle.
- `NetherAutoClimbController` now creates that lifecycle once, removes `_leaseRestoreCompleted`, and uses it at battle entry, clear/close settlement, F12 off, generic fail-closed pause, Result completion, FloorSelection termination, and plugin unload. `PlanStableBoundary` refuses new route selection while the lifecycle has no exact accessor or a recovery/fault/retry phase. Update calls only the coordinator's bounded retry pump; idle frames issue no native write and no detailed log.
- Exact native accessor registration is unchanged in surface and no heuristic was added: `NetherRuntimeBridge.ObservePatchedCall` observes `Project.Ingame.BottomRightView.ApplyUserSettings` and `OnDestroy`; `NetherBattleSettingsNativeRegistry` installs/uninstalls the exact `IIngameUserSettings` accessor and then notifies the controller lifecycle outside its lock. `NetherBattleSettingsLease.RegisterNativeAccessor` no longer restores by itself, so startup recovery cannot run before the exact accessor exists. Destroying the owner clears the accessor but keeps the on-disk lease evidence.
- `NetherRuntimeBridge.UnregisterFloorSelectionCore` signals the controller after releasing its bridge lock. Result and Continue retain their existing separate ownership/task evidence; this signal is only the native settings restore boundary.
- Failed/native-unknown restore results move the coordinator to `RetryWait` or `Paused`; `BlocksRouteOrBattle` then rejects the next route/battle. The retry interval is bounded in update ticks and success requires the existing lease write/readback phase proof before entry reopens. Rebinding an accessor while an active forced lease exists routes through the real persisted recovery path before new route selection can resume.

### TDD evidence

- RED: `NetherBattleSettingsLeaseControllerLifecycleTests` was added first and the fresh focused project failed with five expected `CS0246` errors because `NetherBattleSettingsLeaseControllerLifecycle` did not yet exist.
- GREEN: minimal production lifecycle and scheduled retry implementation made the initial five real-lease/driver tests pass. They cover deferred startup recovery after exact registration, two independent battle acquire/restore cycles, second-battle F12-off and unload restore, bounded non-per-frame retry, restore fault blocking route/next battle, and accessor destroy preserving the persisted lease.
- RED regression: the active-lease rebind test then failed with `Expected: Completed; Actual: Started`. The coordinator was updated to treat a forced/restoring/retry phase on exact re-registration as recovery-required. GREEN proves the rebound native accessor restores the original pair, deletes the persisted lease only after readback, and reopens route/battle only then.
- The characterization target is production code used directly by `NetherAutoClimbController` and the exact native registry; the tests use the real file-backed `NetherBattleSettingsLease` wherever possible and a narrow driver only for controlled retry timing/fault branches.

### Verification before the C2 commit

- Fresh Docker lifecycle focus with `/game:ro` and `/reverse:ro`: `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --filter 'FullyQualifiedName~NetherBattleSettingsLeaseControllerLifecycleTests|FullyQualifiedName~NetherBattleSettingsLeaseRuntimeCoordinatorTests|FullyQualifiedName~NetherBattleSettingsLeaseStateTests'` passed `21/21`, `0` failed/skipped. The test compiler emitted established nullable/fixture warnings only.
- Fresh Docker complete suite: `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --logger 'console;verbosity=minimal'` passed `499/499`, `0` failed/skipped.
- Fresh RO Release build: `ABYSS_GAME_DIR=/game dotnet build AbyssMod/AbyssMod.csproj -c Release -p:BaseOutputPath=/repo/release/nether-auto-climb/` produced `release/nether-auto-climb/Release/net6.0/AbyssMod.dll` with `0` warnings / `0` errors. NuGet retried once after a transient SSL EOF, then restored successfully; `/game` and `/reverse` remained read-only.
- `git diff --check` and `git diff --cached --check` passed. The worktree deliberately still contains the user-owned `M README.md` and `?? build/`; neither was edited or staged. The precise C2 stage contains only the lease/controller/runtime/test/report files named by this milestone.
- C2 commit: `ba84823 fix: coordinate nether battle settings lease`. This commit stages only the C2 lease/controller/runtime/test/report scope; the user-owned `README.md` and `build/` are excluded.

## Fix round 2 — Milestone C3: code semantics, detailed auditing, and production controller E2E

### Authoritative code semantics

- RO evidence used: `reverse_out/cpp2il_latest/DiffableCs/Project/Project/Master/NoaMessagePack/MNetherCodes.cs` exposes the master `category`; `reverse_out/cpp2il_latest/DiffableCs/Project/Project/NetherCodeCategoryType.cs` defines `Technique=1`, `Strength=2`, `ErosionResistance=3`, and `ErosionEnhancement=4`; and `reverse_out/cpp2il_isil/IsilDump/Project/Project/NetherCodeCategoryTypeExtensions.txt` proves the two category groups, Technique/Strength pairing, ErosionResistance/ErosionEnhancement pairing, and same-group exclusivity.
- `NetherCodeCategorySemantics` is production code used by `NetherCodePolicy` and `NetherRuntimeBridge`. Technique and Strength are supported as ordinary `General` candidates, so common ordinary offers no longer pause merely because no static RO evidence distinguishes a Rush from an Impact lane. ErosionResistance maps to Safe and ErosionEnhancement to Risk. Category pair/exclusive conflict, stable tie-break, capacity/replacement, reload reserve, and the documented `30024`/`40024` overrides remain explicit.
- Unknown or malformed category/master/effect fields remain fail-closed. No code ID is guessed as Rush, Impact, party coverage, or research-only: the available RO master/UI artifacts establish categories and erosion effects, but do not establish those semantic mappings. This does not block Technique/Strength offers; it only retains a named pause for an offer whose safety-relevant data is genuinely unknown.
- RED: `NetherCodeCategorySemanticsTests` was added before the production category contracts and failed with `CS0246` for the missing category symbols. GREEN covers all four categories, pair cancellation/exclusive conflicts, `30024`/`40024`, invalid category, capacity/replacement/reserve, and deterministic tie-break. The focused semantic/policy suite is part of the final `27/27` result below.

### Structured DetailedLogging

- New production `NetherDetailedAuditLogger` accepts an injected sink and formats only bounded structured fields. It has per-kind deduplication and a maximum 16 entries per audit kind. Disabled logging returns before formatting/emission, including repeated polling.
- Controller production wiring records bounded snapshot, route candidate/rejection/reverse-reachability, interactive input, battle projection/actual/drift, F11 block, lease transition, checkpoint carry ordering, native invocation, native task terminal, and reconcile classification evidence. It emits no localized display/name/description/raw-payload fields.
- RED: logger tests failed before the production type existed. GREEN tests cover disabled polling silence, the required audit kinds, sensitive-key exclusion, field/value bounds, per-kind dedupe, and the 16-entry cap.

### Real-controller fake-runtime E2E

- The test project links the actual production `AbyssMod/Services/NetherAutoClimbController.cs`; its only test boundary is `INetherRuntimeBridge`, introduced through `PushRuntimeBridgeForTests`. The production static controller rebuilds its real state, settings gate, native-flow, GET-only reconcile, battle-settlement, Continue-scene, and lease coordinators around the supplied bridge, then restores the normal live bridge. Unity/IL2CPP reflection remains outside the test adapter; this is a characterization seam, not a parallel controller.
- `NetherAutoClimbControllerEndToEndTests` drives the real controller through `enable → Play → Event popup → exact GET-only reconcile → route-selected Battle → Sleep/Continue → new segment runtime registration → Result`. It asserts the SelectFloor/Event parent stays pending until terminal, a retained old popup never replays, no native mutation is duplicated, the Continue preflight executes before its native parent, parent/Continue tasks are waited, and two battles each acquire then restore the real settings lease.
- A second E2E path disables F12 while an Event parent is in flight. Repeated off/play requests cannot re-enable it; the native task and one authority GET drain first, then the state becomes disabled with no duplicate invoke.
- RED E2E exposed two real production wiring defects. First, `NetherPopupDispatchPolicy.FromEventDecision` lost the selected event effects, making action-specific GET reconciliation correctly return Ambiguous. `NetherEventDecision.ExpectedEffects` now flows into `NetherPlannedAction`. Second, `ObserveBattle` called `BeginBattleSettlement()` on every pending GET poll, repeating a lease-restore edge; it now executes that transition only on entry to `AwaitingBattleSettlement`.
- GREEN: a fresh Docker E2E-focused command passed `2/2` (`NetherAutoClimbControllerEndToEndTests`). The combined C3 focused command filtered code semantics/policy, detailed audit logging, popup dispatch, and E2E; it passed `27/27`, `0` failed/skipped.

### Final C3 verification

- Fresh Docker command with `/game:ro` and `/reverse:ro`: restored then ran the complete `AbyssMod.Tests` project. Result: `511/511` passed, `0` failed/skipped.
- Fresh RO Release command: `ABYSS_GAME_DIR=/game dotnet build AbyssMod/AbyssMod.csproj -c Release --no-restore -p:BaseOutputPath=/repo/release/nether-auto-climb/`. Result: `0` warnings / `0` errors; output `release/nether-auto-climb/Release/net6.0/AbyssMod.dll`.
- The C3 stage is restricted to the category/runtime/controller/policy/test/report files listed by this milestone. It excludes user-owned `README.md`, untracked `build/`, ignored Release output, and all game/reverse paths. Final diff/status checks are recorded with the C3 commit.

## Review round 3 remediation — lifecycle, authority, and composed interactive transactions

### Review intake and bounded scope

- Both `code-review-round-3.md` and `spec-review-round-3.md` were read in a disposable Docker container before changes. Every unique finding was checked against the linked production controller/bridge seams and the supplied RO ISIL paths; no raw Nether request, Start/resume fallback, or direct game-directory write was added.
- The remediation remains a single-controller design: Harmony/reflection only registers exact native objects/tasks; `NetherAutoClimbController` owns decisions, invokes at most one native flow, waits for the correct parent/child terminal evidence, then uses GET-only reconciliation.

### A–D: battle lifecycle, terminal HP, generation visibility, and interactive budget

- Clean startup without a persisted Auto/speed lease no longer blocks `SelectFloor` merely because `BottomRightView.ApplyUserSettings` has not registered. A live/unrestored persisted lease still blocks. The first battle registers the exact accessor, acquires/forces before battle automation, restores on clear/close/off/leave/unload, unregisters cleanly, and repeats this acquire/restore sequence independently for the second battle.
- Battle, MiniBoss, and Boss all receive the configured minimum HP gate. Necessary terminal status only exempts the 90-point soft erosion threshold; it never exempts the HP minimum. The 299/300 and recovery-alternative production coordinator paths are characterized.
- Runtime generation observation now explicitly reports absent FloorSelection as generation `0`, while retaining its monotonic internal counter. A deliberate teardown may span update ticks before a new `N+1` registration; stale live owner/generation still pauses rather than being accepted as a rebind.
- Interactive pre-entry stores each server-possible event row's selected option projection and feeds the worst erosion/HP consequence into reverse terminal reachability. Thus the `erosion 20 + Event +60 + Boss +20` route is rejected rather than treating only the selected row as a budget, while safe all-row controls remain selectable.

### E–F: authoritative Code facts and exact one-ticket Boost flow

- `NetherCodeState` now distinguishes `PartyCoverageKnown` and `IsResearchOnlyKnown` from their values. `General` is no longer evidence of a Rush/Impact lane, nor are `0`/`false` evidence of coverage/research semantics. A Technique/Strength ordinary offer whose required lane/coverage/research fact is unresolved pauses with an identifier-only detailed audit and no select/reload/replace native callback. Safe/Risk actions are allowed only when their decision does not consume an unknown fact; capacity removal or locked-lane reasoning with an unknown stays blocked.
- Raw bridge/master mapping characterization covers that unresolved case, the four static categories, pair/exclusive behavior, `30024`/`40024`, invalid data, capacity/replacement/reload reserve, and stable tie-breaks. No semantic ID was invented.
- RO-confirmed Continue wiring is now used for both `_canBoost` branches: `NetherContinueConfirmPopupController.<>c.<SetupPopupEvent>b__8_2` opens/owns the Continue chain; with Boost it waits for a fresh owner-scoped Boost popup and invokes `NetherBoostConfirmPopupController.<>c.<SetupPopupEvent>b__7_2` with count `1`, then `b__7_1` confirmation. The false branch uses the exact one-ticket native continuation. The bridge never synthesizes request parameters, does not reuse stale Boost registrations, and has bounded timeout/duplicate-frame guards.
- Evidence: `reverse_out/cpp2il_isil/IsilDump/Project/Project/Nether/NetherContinueConfirmPopup/NetherContinueConfirmPopupController_NestedType___c.txt` (`b__8_2`), its generated setup state machine (Boost popup), and `NetherBoostConfirmPopupController_NestedType___c.txt` (`b__7_2` count then `b__7_1` confirm).

### G: immutable route-selected noncombat transactions

- `NetherFloorActionTransactionComposer` replaces an originally provisional `SelectFloor` settlement contract only after an owner-matched child decision is exact. The original `RuntimeFlow.ParentAction` and parent `UniTask` stay intact; `NetherAutoClimbStateMachine.TryReplacePendingFloorTransaction` atomically preserves the pre-action fingerprint while enriching the settlement copy with floor id, popup kind, option/content, amount/cost/effects, code/replacement, and exact terminal status.
- Event/Recovery/Treasure use exact selected effect contracts. Event-triggered Battle requires `Battle`; Recovery includes exact Heal/Item/code deltas; Treasure verifies keys. Shop Leave requires the selected floor/status terminal, and Shop Buy additionally requires exact content amount and Nether-gold cost. Code select/reload keeps the exact code/replacement/reload postcondition. A wrong floor, status, item, HP, code, ticket, map, or resource remains `Ambiguous`, never `Applied`.
- Production fake-bridge controller paths cover five owned interactive route chains: Event, Recovery, Treasure, Shop Leave, and Shop Buy. Each begins at Play, selects exactly one floor, drives only the owner-matched popup, waits the original parent terminal, performs one GET-only reconciliation, and reaches Stable only on its action-specific contract. Composer/policy/state mutation regressions cover event-triggered Battle and code as well.

### Recovered direct-Wait Event safety boundary

- A recovered `Wait` Event/Recovery/Treasure popup without a controller-owned `SelectFloor` parent now has the named stop `BindingUnavailable:direct-wait-event-parent-task-unavailable`, performs **zero** native callbacks and **zero** GETs, and is Paused rather than reported Stable. The pause transition is DetailedLogging-gated and deduplicated by `LogTransition`/`NetherDetailedAuditLogger`; logging-off remains completely silent.
- This is deliberate, not an unimplemented raw fallback. RO evidence: `NetherEventPopupController.ExecuteEvent` is `void` and dispatches a `UniTask.Void` callback (`NetherEventPopupController.txt:2589-2700`); the generated `ExecuteEvent_b__0_d.MoveNext` later calls `_onConfirm` but returns no recoverable task (`...ExecuteEvent_b__0_d.txt:138-254`). The sole exact parent `UniTask` is `SubViewController.OnFloorClickedEventAsync(int,int)`, captured only at the original SelectFloor invocation. Its generated state machine is transient and owns no popup identity, so a late global `MoveNext`/`onConfirm` observer cannot correlate a recovered popup to a unique server action without risking a duplicate or premature GET.
- The minimum live/native context required to automate this special recovered state safely is an exact, owner-correlated terminal task/cancellation contract retained after the original click (or a game-provided callback carrying the parent generation and terminal result). Until then the zero-mutation pause is the safe terminal behavior.

### H: bounded diagnostics and end-to-end coverage

- Detailed audit remains opt-in and bounded: snapshots, route candidates/rejections/reverse reachability, interactive transaction payloads, battle projection/actual drift, F11 blocks, lease transitions, checkpoint carry ordering, native invoke/task terminal, and reconcile classifications use identifier/numeric fields only. `NetherDetailedAuditLogger` has per-family limits and exact formatted-entry deduplication, while disabled logging returns before formatting/polling work.
- The real production-controller fake-runtime test target now has ten scenarios: full lifecycle (`enable → Play → battle → Sleep/Continue → segment rebind → Result`), persisted-lease blocker, unknown Code no-callback, the five owned noncombat contracts, recovered direct-Wait zero-mutation pause, and off-mid-flight drain. It compiles the actual controller, bridge interface, state machine, runtime-parent coordinator, GET-only reconcile, Continue handoff, and battle-settings lease seams rather than testing a parallel helper.

### RED/GREEN and final verification

- RED roots caught by this round included: clean route incorrectly blocked by an absent accessor; boss HP terminal exemption; generation disappearance treated as runtime loss; interactive reverse budget ignoring a possible row; unknown Code facts silently defaulted; the Boost branch staged but not invoked; and owned noncombat parents retaining `ExpectedAfterStatus=Unknown`. Production-chain tests were added before each small fix and mutation cases exercise the removed permissive path.
- G's first production RED paused on the intentionally strict `ExpectedAfterStatus=Unknown`; composing the immutable child settlement contract made the five exact route chains GREEN. The late direct-Wait investigation did **not** justify a mutation: the RO method signatures above prove no correlatable awaitable, so its regression is GREEN as a named zero-mutation pause.
- Fresh Docker focused aggregate with `/game:ro` and `/reverse:ro` passed `132/132`, `0` failed/skipped. It includes controller E2E (10 scenarios), floor transaction composer/reconcile/state/ownership, Code category/runtime diagnostics, Boost/checkpoint paths, lease lifecycle, and interactive route safety.
- Fresh Docker complete test suite passed `547/547`, `0` failed/skipped. Existing nullable/fixture/analyzer warnings remain in `TranslationPaths`, `Manifest`, `StaticBundleProtocol`, reflection fixtures, and two pre-existing xUnit `Where` assertions; no test failed.
- Fresh RO-game Release verification restored `AbyssMod/AbyssMod.csproj` then ran `ABYSS_GAME_DIR=/game dotnet build AbyssMod/AbyssMod.csproj -c Release --no-restore -p:BaseOutputPath=/repo/release/nether-auto-climb/`; result `0` warnings / `0` errors, artifact `release/nether-auto-climb/Release/net6.0/AbyssMod.dll`. The first `--no-restore` attempt failed only with `NETSDK1127` in a new ephemeral NuGet cache; the subsequent project restore exposed and fixed a missing production `using System.Linq` for an existing `.ToArray()` call before the green build.

## Review round 4 remediation — durable lease discovery and owned modal chains

### Verified production changes

- `NetherBattleSettingsLease` now separates a read-only persisted-file probe from native recovery. `NetherBattleSettingsLeaseControllerLifecycle.OnControllerInitialized()` can classify no-file, valid active, and corrupt/unreadable lease state before a `BottomRightView.ApplyUserSettings` accessor exists. No-file leaves routes available; an active or unreadable durable record blocks SelectFloor. Only `OnExactAccessorRegistered()` attaches the exact native accessor, restores/readbacks/deletes a valid lease. Retry of a corrupt/unreadable record is bounded and remains read-only until it can prove a clean state.
- `NetherFloorActionTransactionComposer` retains the immutable SelectFloor parent/pre-snapshot and appends owner-matched stages instead of replacing the last child. Duplicate effect keys, stale owner/generation/sequence, contradictory code selection, or an incomplete child stage cannot reach reconciliation. `NetherActionReconcilePolicy` evaluates every appended stage in one authoritative GET, so Event gold/effect plus a required code change must both match.
- `NetherShopPurchaseCloseCoordinator` serializes exact `OnPurchaseContentAsync` task observation → the registered same-owner Shop close delegate once → the original floor-parent task. It has sticky fault/timeout behavior: no retry or close after an unknown child/close result. `NetherRuntimeBridge.InvokeExactShopPurchaseClose()` validates owner action/generation/sequence/liveness immediately before invocation.
- `NetherCodeReloadEpochCoordinator` binds the RO-confirmed private `AbyssCodeSelectPopupController.RerollAsync(AbyssCodeSelectPopup)` child. It waits the boxed native task, captures the still-live same-owner popup again, requires exactly one reload count decrement and a changed complete candidate fingerprint, then advances only its decision epoch. `NetherRuntimeFlowCoordinator` permits the same CodeOffer sequence to redispatch only at a strictly newer epoch. An unchanged offer, stale owner, unknown candidates, fault, or timeout is sticky fail-closed; a Reload-only transaction is incomplete and cannot issue GET.
- The controller keeps these parent/child chains while F12 is off, drains to a single exact reconciliation when terminal evidence exists, and rejects an off→on repeat while evidence remains. No raw endpoint or synthetic mutation path was added.

### TDD and characterization evidence

- RED: the initial Round 4 reload tests failed to compile because the epoch coordinator/owner contracts did not exist. GREEN introduced the production coordinator and exact bridge wiring. A later RED showed a Reload-only stage could be treated as complete; `IsCompleteForParentTerminal` now requires an owner/sequence-matched higher-epoch terminal `SelectCode` stage.
- New component regressions cover reload fresh-fingerprint/count proof, unchanged/count mismatch, stale owner, task fault, and bounded pending timeout; Shop regressions cover child/close fault and bounded timeout with zero close/retry. Runtime-flow stale-generation coverage now counts the dispatch delegate itself, rather than a fake-driver counter that could not observe a callback.
- `Production_controller_appends_event_then_code_popup_under_one_parent_and_one_get` and `Multi_stage_event_then_code_parent_requires_both_effect_contracts_from_one_get` verify both halves; wrong gold or wrong selected code is `Ambiguous`. `Production_controller_pauses_before_get_when_parent_terminal_lacks_required_code_stage` proves incomplete transaction evidence cannot start GET.
- Shop and Reload controller E2Es now opt into a truthful `RequireExplicitFloorParentTerminal` seam. They preserve the original parent as pending after Buy→Close or Reload→fresh epoch→Select, assert zero GET until the test explicitly completes that parent, and then assert exactly one parent terminal and one GET. The same paths disable F12 mid-child and prove disabled drain with exactly one Buy/Close or one Reload/Select and no re-enable/replay.
- Real file-backed lease lifecycle tests cover a valid active file (zero native write before registration, restore/readback/delete after registration), no file (routeable without an accessor), and corrupt file (block/retry/read-only clean release). These tests use temporary lease files and the production lease/lifecycle, not an injected `NeedsRecovery` boolean.

### Fresh verification

- Fresh copied-repository Docker focused aggregate with `/game:ro` and `/reverse:ro` filtered `NetherFloorActionTransactionComposerTests`, `NetherCodeReloadEpochCoordinatorTests`, `NetherShopPurchaseCloseCoordinatorTests`, `NetherActionReconcilePolicyTests`, `NetherAutoClimbControllerEndToEndTests`, `NetherRuntimeFlowCoordinatorTests`, `NetherBattleSettingsLeaseControllerLifecycleTests`, and `NetherBattleSettingsLeaseRuntimeCoordinatorTests`: `68/68` passed, `0` failed/skipped.
- Fresh copied-repository Docker complete suite: `570/570` passed, `0` failed/skipped. Test compilation retains the established nullable/reflection-fixture/xUnit-analyzer warnings; no test failed.
- Fresh RO game build used `ABYSS_GAME_DIR=/game` and `BaseOutputPath=/repo/release/nether-auto-climb/`: Release succeeded with `0` warnings / `0` errors; artifact `release/nether-auto-climb/Release/net6.0/AbyssMod.dll`.
- The only intentional live-only boundary remains the previously accepted recovered direct-Wait Event/Recovery/Treasure path: without the original owner-correlated floor parent `UniTask`, it pauses as `direct-wait-event-parent-task-unavailable` with zero native mutation and zero GET. No speculative late `MoveNext` observer was added.
- Independent post-audit expanded focused verification (all `NetherBattleSettingsLease*` plus the composer, reconciliation, Shop/Reload, runtime-flow, and controller E2E suites) passed `75/75`, `0` failed/skipped. A second fresh copied-repository complete-suite run remained `570/570`; the independent fresh Release build remained `0` warnings / `0` errors.

## Review round 5 remediation — multi-reload Code chains and shared native stage runtime

### Wave A — cumulative reload and root-terminal composition

- The transaction composer now accepts an owner/generation/sequence-matched sequence of strictly advancing Code decision epochs. Each Reload must prove a fresh candidate fingerprint and an exact count decrement; repeated, stale, unchanged, or backward epochs remain fail-closed. At parent terminal, reconciliation aggregates the retained Reload stages with checked arithmetic rather than incorrectly requiring each stage to observe the original pre-count.
- A terminal `SelectCode` remains required after one or more Reload stages. The production controller seam covers `count=3 → Reload → count=2 → Reload → count=1 → Select → original parent → one GET`, including off/fault/stale no-replay cases.
- Event children now inherit the immutable root SelectFloor terminal contract. An Event with resource effects plus Code change followed by Battle therefore retains all resource, Code, and root-Battle postconditions until the one original parent terminal and the one authority reconciliation. Partial resource, code, or battle mismatch pauses as Ambiguous.

### Wave B — exact Keep/cancel terminal

- `KeepCode` is an explicit terminal Code stage. The only native binding is the RO-confirmed `AbyssCodeSelectPopupController.<SetupPopupEvent>b__12_0` cancel chain through `NetherUtility.<OpenAbyssCodeSelectPopupIfNeededAsync>g__HandleCancelSequenceAsync|19_3` to `RequestNetherFixCodeAsync(0,0)`.
- The callback itself is fire-and-forget, so the bridge observes and owner-correlates the generated cancel task. It waits that task, then the original SelectFloor parent, then performs one GET. It never treats visual close as completion. Direct Keep, Reload→Keep, and Reload→Reload→Keep retain the same owner/sequence; stale, off, fault, timeout, and duplicate paths do not replay cancellation.
- Keep reconciliation requires the pre-action Code portfolio to remain unchanged and applies only the aggregate retained Reload decrement. A partial count or portfolio result is Ambiguous, never Applied.

### Wave C — one production owned-popup dispatch and parent gate

- `NetherOwnedPopupNativeStageRuntime` is the sole state machine for Shop Buy child/close, Code Reload epoch refresh, and Code Keep cancel observation. `NetherOwnedPopupStageBridgeEntry` owns that runtime and exposes the single typed dispatch table and the only parent-poll gate.
- The reflection bridge now inherits the non-virtual `NetherOwnedPopupStageBridgeAdapter`; it supplies only owner matching and exact native primitive callbacks via `INetherOwnedPopupNativeStagePort`. It no longer retains a local Shop/Reload/Keep switch, coordinator, runtime, or parent-gate state machine. The scripted production-controller seam delegates to the same shared entry rather than reproducing the sequence.
- Disposable-copy mutation audit demonstrates that the targeted controller/core tests execute the production shared code: changing the shared Shop route made `4/29` tests fail, Reload `7/29`, and Keep `8/29`. Before this extraction, mutating the old real-Bridge delegation route left `29/29` targeted tests passing; that gap is why the shared adapter is now the actual product path.

### Round 5 verification and retained boundary

- Fresh copied-repository focused coverage for BridgeEntry/core/controller/Keep/Shop/Reload/runtime-flow passed `49/49`, `0` failed/skipped. Fresh copied-repository full suite passed `601/601`, `0` failed/skipped. The usual nullable/reflection-fixture/analyzer warnings remain non-fatal.
- Fresh RO-game Release verification used `ABYSS_GAME_DIR=/game` with `/game:ro` and `/reverse:ro`; it completed with `0` warnings / `0` errors. `git diff --check` passed before staging.
- The only intentional live-only boundary remains the reviewer-accepted recovered direct-Wait Event/Recovery/Treasure popup with no original owner-correlated floor-parent task. It pauses as `direct-wait-event-parent-task-unavailable` and performs zero native mutation and zero GET; no speculative late state-machine observer is installed.

## Round 6 — exact packaged Code-terminal interop binding

### Packaged interop evidence and fail-closed resolver

- A disposable Docker characterizer loaded the read-only `/game/BepInEx/interop/Project.dll` in an isolated `AssemblyLoadContext` (with dependencies resolved only from `/game/BepInEx/interop` and `/game/BepInEx/core`). It verified the current package exposes the public nested holder `Project.Nether.AbyssCodeSelectPopup.AbyssCodeSelectPopupController.__c`, its static singleton property `__9`, and the public instance callbacks `_SetupPopupEvent_b__12_3` (detail), `_SetupPopupEvent_b__12_2` (confirm), and `_SetupPopupEvent_b__12_0` (cancel).
- The same characterization uniquely resolved the exact static `Project.Nether.NetherUtility` task factories:
  - confirm: `Method_Internal_Static_UniTask_AbyssCodeSelectPopupController_Int64_NetherPartyModel_CancellationToken_0(controller, Int64, NetherPartyModel, Il2CppSystem.Threading.CancellationToken) -> UniTask`;
  - cancel: `Method_Internal_Static_UniTask_AbyssCodeSelectPopupController_CancellationToken_0(controller, Il2CppSystem.Threading.CancellationToken) -> UniTask`.
  No relevant `ObfuscatedNameAttribute` is present on this packaged version. The resolver still accepts a future exact attribute match, but never uses an arity-only, substring, raw cpp2il-name, `<>c`, `<>9`, or `System.Threading.CancellationToken` fallback. Zero or multiple exact candidates return `BindingUnavailable`.
- `NetherRuntimeBridge.SelectCode` now sends detail and confirm through `NetherCodePopupInteropResolver`; Keep sends cancel through that same resolver. `GetCodeSelectionTaskPatchTarget()` and `GetCodeKeepCancelTaskPatchTarget()` resolve the two generated task factories through the matching bindings. The legacy generic generated-callback helper remains only for the unrelated checkpoint callbacks.
- `PatchManager.Initialize()` registers `NetherAutoClimbCodeSelectionLifecyclePatch` and `NetherAutoClimbCodeKeepCancelLifecyclePatch` exactly once. Their `TargetMethod` implementations delegate to the corresponding bridge target methods, so the observer chain remains native callback → exact generated UniTask → owner-correlated parent gate → one GET-only reconciliation.

### Reconcile hardening

- `NetherActionReconcilePolicy` now requires exact checked aggregate reload arithmetic for every composed Code terminal (`SelectCode` or `KeepCode`), including the direct/no-reload case `after.CodeReloadCount == before.CodeReloadCount`. A direct SelectCode with a decrement, a composed Select/Keep with no Reload stage but a decrement, or an aggregate mismatch is `Ambiguous`; it cannot be reported Applied. Owned child Select stages prove the portfolio only, leaving the sole aggregate resource proof to the parent transaction's final authority snapshot.

### TDD and mutation evidence

- RED: a fresh copied-repo focused run first produced `3` failures out of `19`: Keep-cancel was absent from `PatchManager`, and both direct SelectCode and composed zero-Reload Select/Keep incorrectly accepted a reload-count decrement. The failures were fixed only after adding the registration and exact arithmetic above.
- GREEN: `NetherCodePopupInteropResolverTests`, `NetherCodePopupNativeBindingTests`, lifecycle registration/target contract tests, reconcile tests, shared owned-popup runtime/entry tests, and the real controller E2E seam passed `65/65` in a fresh Docker copy. The package test is permanent and reads `/game` each run; it verifies holder/singleton, all three callbacks, and both task methods without attempting to initialize an IL2CPP singleton outside the game.
- Disposable Docker-copy mutations were detected: replacing the sanitized confirm callback with the raw cpp2il name failed the packaged characterizer; replacing the IL2CPP token with `System.Threading.CancellationToken` failed it; deleting the Keep-cancel registration failed the registration regression. Thus the tests do not silently accept the old raw-name path.
- Production ordering is covered jointly rather than by manually completing a bridge observer: package resolution proves the actual targets, the registration contract proves Harmony installs both observers, and `NetherOwnedPopupNativeStageRuntime`/`NetherOwnedPopupStageBridgeEntry` plus controller E2E prove the owner → child task → original parent → one GET gate. The native callback itself is not invoked in a non-game test process.

### Fresh verification before commit

- Fresh copied-repo focused Docker test command filtered `NetherCodePopupInteropResolverTests`, `NetherCodePopupNativeBindingTests`, `NetherCodeLifecyclePatchRegistrationTests`, `NetherActionReconcilePolicyTests`, `NetherOwnedPopupNativeStageRuntimeTests`, `NetherOwnedPopupStageBridgeEntryTests`, and `NetherAutoClimbControllerEndToEndTests`: `65/65` passed, `0` failed/skipped. Existing nullable/reflection-fixture/analyzer warnings remain non-fatal.
- Fresh copied-repo complete suite: `dotnet test AbyssMod.Tests/AbyssMod.Tests.csproj --logger 'console;verbosity=minimal'` passed `612/612`, `0` failed/skipped.
- Fresh read-only game Release build: `ABYSS_GAME_DIR=/game dotnet build AbyssMod/AbyssMod.csproj -c Release -p:BaseOutputPath=/tmp/abyss-round6-output/` succeeded with `0` warnings and `0` errors. `/game` and `/reverse` remained mounted read-only; no game DLL was changed.
- The retained boundary is unchanged: a recovered direct `Wait` Event/Recovery/Treasure popup lacking the original owner-correlated parent task pauses as `direct-wait-event-parent-task-unavailable`, with zero native mutation and zero GET.
