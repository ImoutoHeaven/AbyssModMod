# SDD ledger — plan: docs/superpowers/plans/2026-08-07-nether-auto-climb.md

- Design/spec commit: `bc84a2f docs: design nether auto-climb`
- Baseline: Docker `dotnet test`, 199 passed, 0 failed, 0 skipped; pre-existing nullable warnings recorded.
- Workspace: current authorized `master` checkout; preserve modified `README.md` and untracked `build/`.
- Persistent implementer: `/root/nether_flow_api_audit`.
- Persistent spec reviewer: `/root/nether_mechanics_audit` (activate only after implementation).
- Persistent code reviewer: allocate after implementation; do not replace any of these sessions.
- Implementation status: in progress.
- Task 1 complete — `5e7ebe0 feat: add nether auto-climb state model`; RED/GREEN/full-suite evidence appended to `implementation-report.md` (208 passing).
- Task 2 complete — `bc53c9e feat: add safe nether route planning`; RED/GREEN/full-suite evidence appended to `implementation-report.md` (217 passing).
- Task 3 complete — `b650b4e feat: add nether erosion and code policies`; RED/GREEN/full-suite evidence appended to `implementation-report.md` (238 passing).
- Task 4 complete — `1bf7d22 feat: add safe nether node action policy`; RED/GREEN/full-suite evidence appended to `implementation-report.md` (251 passing).
- Task 5 complete — `17df2e4 feat: add nether checkpoint resource policies`; RED/GREEN/full-suite evidence appended to `implementation-report.md` (266 passing).
- Task 6 complete — `2f2f320 feat: bridge nether native runtime flow`; exact fail-closed bridge/patch bindings added after the supplied `RerollAsync` evidence, focused selector tests passed 11/11, full pure suite passed 268/268, and the RO-game Release build produced `release/nether-auto-climb/AbyssMod.dll` with 0 warnings/errors. Event target-type and live popup timing remain explicit first-real-run fail-closed boundaries.
- Task 7 complete — `a54d911 feat: coordinate nether battle automation`; lease-state RED/GREEN passed 7/7, full suite passed 275/275, and the RO-game Release build passed 0 warnings/errors. Auto/highest-speed native access remains deliberately unregistered pending an exact controller binding; F12 will fail closed rather than guess or issue a raw request.
- Task 8 complete — `5cd390e feat: add F12 nether auto-climb`; integration RED/GREEN added a stable settings gate, focused state/checkpoint tests passed 25/25, full suite passed 281/281, and a clean RO-game Release build produced `release/nether-auto-climb/AbyssMod.dll` with 0 warnings/errors. Runtime is intentionally fail-closed pending exact Auto/highest-speed access and per-node/popup effect mappings; no raw Nether API fallback exists.
- Review round 1 remediation — implementation complete except the explicitly fail-closed Code semantic mapping NEEDS_CONTEXT documented below. Critical/Important and spec-related Minor findings were addressed with exact native controller/task bindings, scene cleanup, battle-scoped settings lease, map/popup/reconcile seams, checkpoint ordering, and bounded detailed audit logging. RED/GREEN additions: code replacement native-flow `3/3`; code-master audit logger `2/2`; combined runtime/state/checkpoint focus `28/28`; final Docker full suite `308/308`. Fresh RO-game Release build is green (`0` warnings/errors) at `release/nether-auto-climb/Release/net6.0/AbyssMod.dll`. No game files, `README.md`, or `build/` were touched.
- Remaining NEEDS_CONTEXT (functional, not a test/build failure): current RO master/ability/UI artifacts expose MNetherCodes category/effect parameters and ability structural types but no verified semantic mapping for ordinary Safe/Risk/Rush/Impact, party coverage, or research-only code effects. F12 therefore pauses rather than guesses on ordinary unmapped code offers; DetailedLogging emits a maximum eight-entry identifier-only `code-master-audit` to collect the minimal live evidence needed to complete this mapping.
- Review round 1 commit: `f46bdd69f583ba822fbb3b576d42ae393661870b fix: harden nether auto-climb runtime flow`. Post-commit fresh Docker verification: `308/308` tests passed; RO `/game:ro` Release build `0` warnings / `0` errors; `git diff --check` and cached diff check passed. Final status contains only the preserved user `M README.md` and `?? build/`.
- Fix round 2 — Milestone A complete — `7f8245b fix: own nether floor modal flow`: production `NetherRuntimeFlowCoordinator` now owns a SelectFloor parent task and its action/generation/sequence-scoped modal children; exact close/destroy invalidation prevents stale replay and out-of-order close corruption. Code confirmation is bound to the proven `b__12_2` callback rather than cancel `b__12_0`. RED mutation coverage and the real-controller seam are recorded in `implementation-report.md`; fresh Docker focused tests passed `7/7`, full suite passed `315/315`, and RO `/game:ro` Release build passed `0` warnings / `0` errors. Milestone B/C are deliberately not changed.
- Milestone A post-commit recheck: new Docker full suite remained `315/315`; RO `/game:ro` Release build remained `0` warnings / `0` errors; both diff checks passed and git scope remained exactly the user `M README.md` and `?? build/`.
- Fix round 2 — Milestone B1 complete — `c9d2dee fix: reconcile nether battle settlement`: GET-only authority reconciliation uses the RO-confirmed `NetherDataStore.SyncNetherDataAsync(CancellationToken)` path; target-specific action policy and explicit battle-settlement lifecycle now drain safely on F12 off. Focused `39/39`, full `333/333`, and RO `/game:ro` Release build `0` warnings / `0` errors. B2/C (continue scene handoff, route safety, lease phase work) remain intentionally untouched.
- Fix round 2 — Milestone B2 complete — Result ownership, bounded Continue scene handoff, checkpoint popup gates, and authoritative Return preflight are now committed as one logical B2 change. FloorSelection teardown preserves Result/Continue evidence; Continue waits for exact parent terminal → teardown → new NetherTop registration → one GET-only reconcile with ticket/map/floor/segment postcondition. Every Continue/Boost/Return/ReturnScroll stage observes its parent and is bounded. Before the native Continue parent starts, live `NetherDataStore.GetOtherAcquiredItemDataList()` plus `NetherPointData.LockReward` and `MItems` master data produce a whole-entry/hash contract; the fresh Return popup/scroll must match it before `OnConfirmAsync`. Fresh Docker verification: B2 aggregate `83/83`, full suite `383/383`, RO `/game:ro` Release build `0` warnings / `0` errors. A live `_canBoost=true` popup is deliberately fail-closed because RO proves `b__8_2` opens boost while `b__8_1` is cancel; no verified native non-boost callback exists.
- Fix round 2 — Milestone C1 complete — `477a073 fix: wire nether route safety authority`. Production route safety now uses exact `MNetherMapFloors` bounds, live party HP ratios, active code erosion effects/hash, battle projection/calibration, and complete interactive pre-entry proof. `NetherAutoClimbController.PlanRoute` consumes the all-or-nothing RuntimeBridge interactive capture through `NetherRouteSafetyProductionCoordinator`; missing/duplicate/stale/unsafe Event, Recovery, Shop, or Treasure data remains unknown and cannot enter the route. Fresh Docker C1 safety aggregate passed `111/111`; full suite passed `485/485`; RO `/game:ro` Release build passed `0` warnings / `0` errors. `git diff --check` passed. The only preserved worktree exclusions remain user `M README.md` and untracked `build/`.
- Fix round 2 — Milestone C2 complete — C2a/C2b use a per-battle persisted-settings lifecycle coordinator rather than `_leaseRestoreCompleted`: exact accessor registration starts recovery, battle entry acquires, clear/close/off/floor-owner termination/result/unload restore, and failed recovery blocks new route/battle with a bounded update retry. Fresh Docker lifecycle focus passed `21/21`, full suite `499/499`, and RO `/game:ro` Release build `0` warnings / `0` errors. No native binding gap was found; the existing exact `BottomRightView.ApplyUserSettings`/`OnDestroy` bridge points are used.
- Fix round 2 — Milestone C2 commit — `ba84823 fix: coordinate nether battle settings lease`; only the C2 lease/controller/runtime/test/report files were staged. User `M README.md` and untracked `build/` remain excluded.
- Fix round 2 — Milestone C3 complete — authoritative code category semantics, bounded structured DetailedLogging, and a fake-runtime end-to-end characterization target now exercise the real `NetherAutoClimbController` through its narrow runtime-bridge seam. Category evidence maps Technique/Strength into a selectable General lane and ErosionResistance/ErosionEnhancement to Safe/Risk; unverified Rush/Impact/party/research semantics are not guessed. The controller E2E covers Event parent/modal ownership, action-specific GET reconciliation, two independently restored battles, one-ticket Sleep/Continue preflight and handoff, Result, stale-popup suppression, and F12-off drain. Fresh Docker focused aggregate passed `27/27`, full suite `511/511`, RO `/game:ro` Release build `0` warnings / `0` errors, with artifact `release/nether-auto-climb/Release/net6.0/AbyssMod.dll`.
- Review round 3 remediation complete — A–D close clean-vs-persisted lease lifecycle, all-combat HP gating, absent/rebind generation visibility, and worst-case interactive reverse budget; E–F make Code facts explicit-unknown and bind the real one-ticket Boost continuation; G composes owner-matched Event/Recovery/Treasure/Shop/Code child contracts into the immutable SelectFloor parent; H extends bounded detailed audit and production controller E2E. A direct recovered `Wait` event without the original parent task is intentionally `BindingUnavailable:direct-wait-event-parent-task-unavailable` with zero mutation/GET, not Stable. Fresh Docker focused aggregate `132/132`, complete suite `547/547`, and RO `/game:ro` Release `0` warnings / `0` errors; artifact `release/nether-auto-climb/Release/net6.0/AbyssMod.dll`. User `README.md` and `build/` remain excluded.
- Review round 4 remediation complete — pre-accessor persisted-lease discovery is now a read-only durable probe; active/corrupt files block routes until the exact accessor restores or a bounded read-only retry proves a clean state, while no-file startup remains routeable. Floor transactions append immutable owner/generation/pre-snapshot stages, so Event→Code validates both effects and selected code from one parent terminal and one GET. Shop Buy now has a bounded native child → exact close → original-parent chain; Code Reload has an owner-scoped monotonic decision epoch and must redispatch an authoritative changed offer before a terminal code selection. Stale/duplicate/fault/timeout/F12-off paths are fail-closed and never repeat mutation.
- Round 4 tests use a production-controller characterization seam with an explicit parent-terminal gate: Shop and Reload prove that child completion/close or fresh epoch alone cannot auto-complete the `SelectFloor` parent or begin GET. Fresh Docker focused aggregate passed `68/68`; fresh full suite passed `570/570`; fresh RO `/game:ro` Release build passed `0` warnings / `0` errors and wrote `release/nether-auto-climb/Release/net6.0/AbyssMod.dll`. `README.md`, `build/`, reviewer reports, and game/reverse paths remain excluded from the precise Round 4 commit.
- Remaining intentional boundary: a recovered direct `Wait` Event/Recovery/Treasure popup with no original owner-correlated `OnFloorClickedEventAsync` parent task remains `BindingUnavailable:direct-wait-event-parent-task-unavailable` with zero mutation and zero GET; it is a named pause, not Stable.
- Post-audit expanded Round 4 focus (lease lifecycle plus composer/reconcile, Shop/Reload coordinators, runtime flow, and production controller E2E) passed `75/75`; the independent fresh full verification remained `570/570` with the RO Release build at `0` warnings / `0` errors.
- Review round 5 remediation — Wave A/B/C complete: multi-Reload Code transactions retain every strictly advancing owner/epoch stage and aggregate reload-count proof through one parent terminal/GET; Event→Code→Battle keeps its root Battle terminal and all resource/code effects. Exact Keep uses the RO-confirmed cancel task, retains the same parent, and verifies an unchanged portfolio plus aggregated reload consumption. Shop/Reload/Keep sequencing now runs only through the shared production `NetherOwnedPopupNativeStageRuntime` / `NetherOwnedPopupStageBridgeEntry`; the real `NetherRuntimeBridge` inherits its non-virtual adapter and supplies only exact native primitives.
- Round 5 mutation audit in disposable Docker copies: disabling shared Shop routing made `4/29` targeted tests fail; Reload `7/29`; Keep `8/29`. The earlier real-Bridge delegation mutant passed `29/29`, which exposed the testability gap addressed by the shared entry/adapter. The final shared-entry/core/controller/Keep/Shop/Reload/runtime-flow focused aggregate passed `49/49`.
- Round 5 fresh final verification: copied-repo full suite `601/601` passed, RO `/game:ro` Release build `0` warnings / `0` errors, and `git diff --check` passed. User `M README.md`, untracked `build/`, and round-5 review reports remain excluded. The only retained live-only boundary is the reviewer-accepted recovered direct-Wait Event/Recovery/Treasure pause (`direct-wait-event-parent-task-unavailable`): zero native mutation and zero GET, never Stable.
- Round 6 exact Code-terminal interop — the packaged BepInEx `Project.dll` is now characterized through a strict versioned resolver: public nested `AbyssCodeSelectPopupController.__c`, static `__9`, sanitized detail/confirm/cancel callbacks (`_SetupPopupEvent_b__12_3/_b__12_2/_b__12_0`), and exact static `NetherUtility` confirm/cancel UniTask factories with `Il2CppSystem.Threading.CancellationToken`. `NetherRuntimeBridge` invokes only that resolver for Code popup callbacks and patch targets; `PatchManager` registers both confirmation and Keep-cancel observers exactly once. Code-terminal reconciliation now proves reload count `pre - retainedReloadStages`, including zero Reload stages. Fresh focused coverage passed `65/65`; full suite passed `612/612`; fresh RO `/game:ro` Release build passed `0` warnings / `0` errors. Temporary-copy mutations to raw callback names, `System.Threading.CancellationToken`, or the Keep-cancel registration line each failed their regression test. The direct-Wait recovered-popup zero-mutation pause remains the only intentional live-only boundary.
